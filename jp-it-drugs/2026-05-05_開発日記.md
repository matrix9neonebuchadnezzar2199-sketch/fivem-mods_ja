# jp-it-drugs 開発日記

**記録日時**: 2026-05-05（作業完了時点）  
**ベース**: [it-scripts/it-drugs](https://github.com/it-scripts/it-drugs) v1.4.4（GPL-3.0）  
**日本語化担当**: eiho_tsukuyomi

---

## 1. 今回のリリース概要

- **版表示**: `fxmanifest.lua` の `version` を `v1.4.4-ja.1` とした（原作 `v1.4.4` ＋日本語化サフィックス）。
- **目的**: UI ロケールの日本語化、デフォルト言語の `ja` 化、README の和訳・運用メモの整理、リポジトリ表記の統一。

---

## 2. 変更ファイル一覧（要点）

| 種別 | パス | 内容 |
|------|------|------|
| 新規 | `locales/ja.lua` | `Locales['en']` と同一キーで日本語文言を定義（`_U` 関数は `en.lua` のみに重複させない）。 |
| 変更 | `shared/config.lua` | `Config.Language = 'ja'`（コメントで `locales/ja.lua` 参照を明記）。 |
| 退避 | `README.en.md` | 原作寄りの英語 README をリネーム保存。 |
| 新規/差替 | `README.md` | 日本語での概要・インストール・日本語化手順・原作との併用禁止の注意。 |
| 変更 | `fxmanifest.lua` | `author` / `description` / `version` / `repository`（`qb-storerobbery-ja` と同様のリポジトリ URL）。 |
| リポジトリルート | `../README.md` | 収録 MOD 表に `jp-it-drugs` 行を追加（`jp-hospital` と `jp-koban` の間＝名前順）。 |

---

## 3. ロケール設計メモ

- `fxmanifest.lua` の `shared_scripts` は `'locales/en.lua'` のあと `'locales/*.lua'` のため、`en.lua` 末尾の `function _U(...)` がグローバル定義として読み込まれる前提。
- `ja.lua` は **`Locales['ja'] = { ... }` のテーブルのみ**とし、`en.lua` とキー集合を一致させた（原作の綴りミスキー `MENU__INGREDIANT` / `NOTIFICATION__MISSING__INGIDIANT` 等も踏襲）。
- 文字コードは **UTF-8（BOM なし）**。検証時は先頭バイトが `-- ` 相当（`2D 2D 20`）であることを確認。

---

## 4. 運用・リポジトリ周り

- **開発ディレクトリ**: `H:\CURSOR\Dev\fivem-mods_ja\`（`AGENTS.md` 等にある `fivem-mods\` 表記とは実パスが異なる場合あり）。
- **`scripts\deploy.bat`**: `DEV_DIR` が別パスを指している可能性があるため、デプロイは手元の運用に合わせて後追い調整可。
- **Git**: `fivem-mods_ja` で管理。初回 `push` がリモート先行で拒否された場合は `git pull --rebase origin main` 後に `push` する流れで解消済み（記録時点）。

---

## 5. あえて未実施にした項目（第2段階候補）

以下は **v1.4.4-ja.2 以降**で別コミットに分ける想定が安全。

- `shared/config.lua` 内の `Config.Plants` / `Config.Drugs` / `Config.SellingZones` などの **`label` / `displayText` 等のゲーム内表示名**の和訳。
- `items/items.lua` の **アイテム表示名**の和訳。
- `shared/config.lua` の **全項目への日本語コメント**（JP-Mods 共通規約では推奨だが、本作は原作 config が巨大なため段階導入）。

---

## 6. 動作確認チェックリスト（運営者向け）

- [ ] `ensure` 順: `ox_lib` → `oxmysql` → `it_bridge` → `jp-it-drugs`（およびターゲット系）。
- [ ] DB: `it-drugs.sql` インポート済み。
- [ ] インベントリに `items/items.lua` 相当の定義＋画像パス。
- [ ] 原作 `it-drugs` と **同時起動しない**（イベント名互換のため衝突）。
- [ ] F8: 起動時 Lua エラーなし。
- [ ] ゲーム内: メニュー・通知・プログレスバーが日本語になること。

---

## 7. 参照リンク

- 原作: https://github.com/it-scripts/it-drugs  
- 原作ドキュメント: https://docs.it-scripts.com/it-drugs  
- 本リポジトリ: https://github.com/matrix9neonebuchadnezzar2199-sketch/fivem-mods_ja  

---

*以降の作業があれば、別日に `YYYY-MM-DD_開発日記.md` を追記するか、本ファイルに「追記」セクションを足す運用でも可。*

# RefBoard 開発日記 — 2026-05-08 23:24 時点

**記録日時**: 2026-05-08（ローカル） / Sprint 09 **Phase E** 完了・**v0.7.0** リリース作業

## 実施内容（Phase E / v0.7.0）

- **英語ヘルプ**: `web/src/help/en/articles/` に日本語 20 本と同一 slug の記事を追加。`en/index.json`・`en/reverse_index.json` を新設。
- **ロケール連動**: `web/src/utils/helpLocale.ts`（`resolveHelpLocale`）。`HelpView.vue` が UI 言語に応じて `ja` / `en` の Markdown・逆引き・目次 JSON を切替（両方 `import.meta.glob` eager）。
- **検索**: `helpSearch.ts` が `HelpLocale` ごとに Fuse インデックスをキャッシュ（`buildHelpIndex('ja'|'en')`、`searchHelp` に loc 引数）。
- **UI**: 左カラムに「やりたいことから / 目次（トピック順）」タブを追加し、`index.json` ツリーを表示。検索モード時は従来どおり検索結果のみ。
- **コンテキスト `?`**: `ContextHelpPanel.vue` の `reverse_index` を UI 言語に追従。
- **i18n**: `help.tree_tab`、`help.subtitle`（`ja.json` / `en.json`）。
- **ドキュメント**: `CHANGELOG.md` v0.7.0、`docs/sprints/sprint_09_help_phase2.md`（Phase E 完了）、`docs/sprints/sprint_07_uiux_findings.md`、`docs/USER_GUIDE.md` / `USER_GUIDE.en.md`（ヘルプ節）。
- **バージョン**: `package.json` / `fxmanifest.lua` / `web/src/constants/version.ts` を **0.7.0** に更新。
- **検証**: `npm run build`、`npx vue-tsc --noEmit` 成功。
- **Git**: コミット `4970b51`（例: `feat(RefBoard): Sprint09 Phase E（英語ヘルプ20本・目次タブ・v0.7.0）`）、`origin/main` へ push 済み。

## メモ

- 英語記事は手書き訳。必要なら DeepL 等で下訳→人手確認の差し替え可。
- Fuse の検索ヒットは引き続き `reverse_index` 由来のみ（`📚 項目ごと` バッジはツリー二重インデックス未実装のまま）。
- Sprint 09 当初スコープのヘルプ Phase 2（v0.6.5→v0.7.0）はここで一区切り。

## 次の候補（任意）

- デモ GIF（計画では任意）。
- `errorCodeMapper` の専用記事をエラーコード側で増やす検討。
- チャンク分割で `index.js` 肥大化への対応（ビルド警告）。

# RefBoard — 既知の問題（v0.9.0 β）

テスト実施中に**未修正**または**仕様として許容**の課題をここに追記する。修正済みは CHANGELOG に記載し、本ファイルからは削除または「解決済み」セクションへ移動。

| ID | 重要度 | 概要 | 再現手順 / 回避策 | 状態 |
|----|--------|------|---------------------|------|
| INC-2026-05-07 | **高**（解決済み v0.6.2） | **閉じていても NUI が全画面描画**し、ログイン UI を覆って操作不能になる | `SetNuiFocus(false)` でも CEF は全面に残る。Lua は `refboard:setOpen` を送っていたが **Vue が未購読**で常時ランチャー描画だった | **修正**: `refboard:setOpen` でシェル `v-if`、起動時 `setOpen(false)` |
| — | — | （未記録） | — | — |

# RefBoard 開発日記 2026-05-10

## 脆弱性・バグ分析レポートに基づく対応

外部から共有された分析（H-1〜H-4, L-6, M-3, M-4 等）をコードで反映。

### 実装

- **H-1**: `sanitizeHelpHtml.ts` を新設し、`ALLOWED_TAGS` / `ALLOWED_ATTR` / `FORBID_*` を明示。`HelpHoverDialog.vue` / `HelpView.vue` で利用。記事内リンクの `decodeURIComponent` を try/catch。
- **H-3**: `stores/matches.ts` の `addEvent` で `goal` / `pk_goal` / `pk_miss` かつ非 void 時、`teamId` がホーム・アウェイのいずれでもない場合は `null` を返しイベントを追加しない。`MatchDetail.vue` で失敗時トースト（変数名 `t` シャドウ回避で `evTime` に変更）。
- **H-4**: `clockNowMs` と `clockAdjust` 内の経過分を `Math.max(0, nowMs() - clockStartedAt)` で下限ガード。
- **L-6**: `localMatchAdapter.ts` に `computeFieldGoalBreakdown` を追加し、`matchToDetailModel` の前半・後半内訳をゴールイベントの `half` から集計（`extra` は現状 0 のまま）。
- **M-4**: `localPersist.ts` で `QuotaExceededError`（および DOMException code 22）を検出し、クールダウン付きでトースト表示（`ja` / `en` 辞書キー追加）。
- **M-3**: `clearMatchData()` 末尾で `rehydrateStoresAfterLocalStorageMutation()` を呼び、直接呼び出し時も Pinia と整合。
- **L-9 一部**: `matches.test.ts` 新設（addEvent 検証・clock 戻り）、`localMatchAdapter.test.ts` に `computeFieldGoalBreakdown` のテスト追加。

### 検証

- `npx vue-tsc --noEmit`
- `npm test`（43 件）
- `npm run build`

### 未着手（レポート記載のまま別タスク）

- L-5 PK 先攻選択、`useNui` タイムアウト、版数表記（M-9）等。

### v0.5.0 リリース（タグ作成）

- `package.json` / `package-lock.json` / `fxmanifest.lua` / `REFBOARD_UI_VERSION` を **0.5.0** に同期。`README.md` / `README.en.md` バッジ、`CHANGELOG.md`、`HANDOVER.md` を更新。`npm run build` 済み **`web/dist/**`** をコミットに含める。
- Git: `main` 上でコミット後、**annotated tag `v0.5.0`** を作成し `git push origin main` と `git push origin v0.5.0` を実行（完了報告にコミットハッシュ・`git rev-parse v0.5.0` を記載）。

## v0.5.1（L-5 + PK リカバリ）

- `Match.pkFirstTeamId`、`setPkFirstTeam`、`createMatch` 初期化、`matchToDetailModel` の `?? homeTeamId`。
- `MatchDetail.onSetHalf` で `setPkFirstTeam` を `setHalf` の前に実行（`MatchStatusCard` は既に `pkFirstTeamId` を emit）。
- `PenaltyShootoutPanel`: `pk-undo-last` / `pk-reroll-first`、確認モーダル、i18n、ヘルプ `match_penalty_shootout` 追記。
- シード PK 試合に `pkFirstTeamId: home.id`。テスト **49** 件。版数 **0.5.1**、タグ **`v0.5.1`**（`web/dist` 同梱）。

## v0.5.2（緊急）— 0 名 PK 脱出不能の修正＋PK 全体キャンセル

- **入口**: `MatchStatusCard` で PK 選択時に両チーム 1 名以上必須。`pk-validation-failed` → `MatchDetail` で `toast.pk_no_players`（5s）。
- **ストア**: `cancelPenaltyShootout` — 非 void の `pk_goal`/`pk_miss` をループで void 化のあと 1 回 `patch`（`homePkScore`/`awayPkScore` 0、`pkFirstTeamId` null、`currentHalf` `2H`）。
- **UI**: `PenaltyShootoutPanel` に「PK戦をキャンセル」＋確認、`pk-cancel-all`。i18n ja/en、ヘルプ `match_penalty_shootout`、`matches.test.ts` +2（計 **51** 件）。`CHANGELOG` / `HANDOVER` / README バッジ **0.5.2**。タグ **`v0.5.2`**（`web/dist` 同梱想定）。

## v0.6.0 — 緊急パッチ後の整理（A/B/C/D/E まとめ）

- **A（M-1）**: `useNui::postToLua` に `AbortController` で 5 秒タイムアウトを実装。タイムアウトは `{ ok: false, error: 'timeout' }`、その他 fetch エラーは `{ ok: false, error: 'fetch_failed' }` を返す。CEF 応答停止時の UI ハング懸念を解消。
- **B（M-6）**: `PenaltyShootoutPanel` の `confirmUndoLast` / `confirmReroll` / `confirmCancelAll` に `isProcessing` フラグを追加して二重 emit を防止。`record()` は既存ガード（`pid` リセット → 再選択前提）で十分なので変更なし。
- **C（H-2）**: `seedActions.ts` の `SETTINGS_STORAGE_KEY` / `LOCALE_STORAGE_KEY` を `NON_PREFIXED_KEYS` 定数に集約、`clearAllData()` 末尾で `rehydrateStoresAfterLocalStorageMutation()`、`resetIdCounters()` を `resetCountersMemoryOnly()` に切替（直前に `clearAllLocal` で `id_counters` を削除済みなので不要書込みを回避）。
- **D（M-9）**: README ja/en の「v0.4.0 の注意」を「データ管理機能について（v0.4.0 以降）」へ見出し変更し、過去形に整理。
- **E（F-1'）**: 受領 4 枚（ランチャー／チーム管理／試合一覧／試合詳細）を `web/public/help/intro_setup/01〜04-*.png` に配置。`intro_setup.md`（ja/en）に各ステップ後で参照（相対パス `./help/intro_setup/...`）。`sanitizeHelpHtml.ts` の `ALLOWED_TAGS` に `img`、`ALLOWED_ATTR` に src/alt/width/height/loading/decoding を追加（`script`/`iframe`/`on*` は引き続き禁止）。
- **テスト**: `cancelPenaltyShootout` の events 残存（matches.test.ts）、`clearAllData` の rehydrate と id_counters クリーンアップ、`resetIdCounters` vs `resetCountersMemoryOnly` のセマンティクス（`dev/seedActions.test.ts` 新設・最小 localStorage モック）を追加。**51 → 55 件**。
- **HANDOVER**: §3 / §5 / §6 / §7（F-3/F-5/F-7 を Issue 化せず TODO 化）/ §9 / §10 / §11 を v0.6.0 ベースに更新。
- **バージョン**: `package.json` / `package-lock.json` / `fxmanifest.lua` / `REFBOARD_UI_VERSION` を **0.6.0** へ。タグ **`v0.6.0`**（`web/dist` 同梱）。

# 2026-05-05 開発メモ（pls_jobsystem / 共通ツール）

## 完了したこと

- `pls_jobsystem` 日本語化版のリポジトリ取り込み（Lua ロケール `ja`、ハードコード除去、NUI 置換マップ＋専用スクリプト）。
- リポジトリ直下の汎用ツール `tools/apply_nui_i18n.ps1` を追加（`-ModName`、マップ解決: `<MOD>_replacements.json` → `nui_replacements.json`、PS 5.1 互換・ASCII ログ）。
- `tools/README_JA.md` に規約・手順・**FAQ**（apply 後の preview がほぼヒットしない理由、少数ヒットの解釈、`$` / `\` と `.Replace()` 等）を記載。
- ルート `README.md` にツール節、`.gitignore` に `**/web/dist.orig/` を追記。
- `pls_jobsystem/scripts/apply_nui_i18n.ps1` は互換のため維持し、冒頭に汎用版への NOTE を追記。

## 運用上のメモ

- `apply` 済みの `index.js` に対して同じマップで `preview` するとヒットはほぼ出ない（冪等）。検証は `restore` → `preview`。
- 残ヒット例: `by PLS SCRIPTS` のように値が英語のままのクレジット行は意図通り。

## 参照コミット（目安）

- `feat(pls_jobsystem): ...`（日本語化本体）
- `feat(tools): ...`（汎用 i18n ツール）
- `docs(tools): apply_nui_i18n の FAQ を追加`（本日 FAQ 追記）

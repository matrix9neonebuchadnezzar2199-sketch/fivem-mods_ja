# 開発日記 — 2026-05-08 初期セットアップ（ドキュメント）

## 実施内容

- **CC BY-NC-SA 4.0** 継承のため `NOTICE.md` を追加し、ルート `README.md` 先頭に英日クレジットを追記。`LICENSE` は上流のまま維持。
- 親リポは `fivem-mods_ja` のみ。`jp-ps-housing/.git` は存在しないことを確認。
- **upstream 追跡**: `docs/upstream.md`（英語）と `docs/ja/upstream.md`。記録時点の upstream `HEAD` は `eaba693b44a8fc87680fb3b02805694e5b11c5f8`。
- **依存**: `docs/ja/dependencies.md` に必須／推奨／省略可と GitHub URL を整理。
- **i18n / フォント / 翻訳棚卸し**: 設計・方針のみ文書化（実装は未着手）。
- **家具ラベル**: `furniture-labels-todo.csv`（886 行）を `tools/extract-furniture-labels.mjs` で生成。

## 次の候補

1. i18n 機構（`shared/locale.lua` + `locales/*`）の実装  
2. NUI フォント（Noto Sans JP）と `ui` ビルドパイプライン確認  
3. 家具ラベル CSV バッチ和訳 → `config.lua` 反映  

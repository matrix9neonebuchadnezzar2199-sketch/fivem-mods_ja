# 2026-05-15 開発日記

## 2026-05-15 — INSTRUCTION-001〜005 一括実行（実作業セッション）

### 変更ファイル（新規・更新）

- `fxmanifest.lua`（新規・`files` は logo_light / logo_dark / icons）
- `LICENSE`（新規・ルート LICENSE と同一趣旨）
- `README.md`（新規）
- `config.lua`（新規）
- `locales/ja.lua`（新規）
- `shared/utils.lua`（新規）
- `server/framework.lua`（新規・Qbox は `exports.qbx_core:GetPlayer`）
- `server/main.lua` … `server/stats.lua`（新規・スタブ含む）
- `client/main.lua` … `client/effects.lua`（新規・スタブ含む）
- `html/index.html` / `style.css` / `app.js`（新規）
- `html/assets/logo_light.png` / `logo_dark.png`（`image/logo.jpg` を複製・拡張子のみ .png）
- `html/assets/icons/site_background.png`（`image/back.jpg` を複製）
- `sql/install.sql`（プレースホルダ）
- `docs/design.md` / `docs/milestones.md` / `docs/dialogue_script.md`（新規）
- `2026-05-14_開発日記.md`（指示書テンプレに沿った初回メモ）
- 本ファイル `2026-05-15_開発日記.md`

### 完了内容

- INSTRUCTION-001: ディレクトリ・マニフェスト・README・LICENSE・日記（05-14 版メモ）
- INSTRUCTION-002: `config.lua` / `locales/ja.lua` / `shared/utils.lua`
- INSTRUCTION-003: `server/framework.lua`（起動時 `print` で FW 名を常時出力、`MRD9.PayPlayer` / `MRD9.GetIdentifier`）
- INSTRUCTION-004: `image/` から `html/assets/` へコピー（ロゴは同一 JPEG を light/dark 両名に配置。**中身は JPEG のため、表示不具合があれば PNG 再エクスポート推奨**）
- INSTRUCTION-005: 本エントリで記録

### 未完了 / 次にやること

- ~~INSTRUCTION-006~~ → 同日後続エントリで完了（コミット **87d33a0** に含む）
- INSTRUCTION-007 以降: セッション・NPC 等（`docs/milestones.md` 参照）
- 実機確認: `ensure jp-meridian9`、F8 の `[jp-meridian9] resource loaded`

### 参照

- マスター提示: MERIDIAN-9 開発指示書 INSTRUCTION-001〜005
- AGENTS.md（リポジトリ）
- Commit: エントリ執筆時点の `HEAD` は **124e22a**。初回 `jp-meridian9` コミットは後続で **87d33a0**（001〜006 一括）

---

## 2026-05-15 — 修正方針の確定（マスター承認・リポ反映）

### 変更ファイル

- `docs/FORMAL_POLICIES.md`（新規：日記例外・グローバル許容・fxmanifest 補足・INSTRUCTION-003/004/006/019 確定文面）
- `server/framework.lua`（FW 検出 `print` 直上に「完了判定用・将来削除候補」コメント）
- `README.md`（開発運用セクション、`FORMAL_POLICIES.md` への導線）
- `docs/design.md`（`FORMAL_POLICIES.md` への参照）
- `docs/milestones.md`（INSTRUCTION-019 とチェックリストの紐付け）
- 本ファイル（追記）

### 確定内容（要約）

- 日記は **MOD 直下 Markdown** のみ（`dev-diary-required.mdc` の HTML 既定は本 MOD では不適用）。
- Qbox は **`exports.qbx_core:GetPlayer(src)`**・qb-core とは分岐分離（実装は既に準拠。`paymentType` の `custom` は実装側で分岐）。
- 画像は **同一複製可・JPEG を `.png` 名で暫定可**、本番問題時は真の PNG 再エクスポート（INSTRUCTION-019 チェックリストに記載）。
- `fxmanifest` の **`license` / `repository`** はリポジトリ慣習として維持（既存）。
- グローバルは **`MRD9` / `Config` / `Locales` / `_` のみ許容**（他は `local`）。

### 次セッション

- **INSTRUCTION-008** 以降。006 までの変更はコミット **87d33a0**、日記ハッシュ表記の同期は **2b89e28**。

### 参照

- マスター確定文面：修正方針（日記・Qbox・画像・fxmanifest・グローバル例外・006 前提）
- Commit: **87d33a0**

---

## 2026-05-15 — INSTRUCTION-006 実行（DB スキーマ・契約/統計 API）

### 変更ファイル

- `fxmanifest.lua`（`dependencies { 'oxmysql' }`、`@oxmysql/lib/MySQL.lua`、`server_scripts` 読み込み順）
- `sql/install.sql`（`mrd9_contracts` / `mrd9_stats` / `mrd9_mission_logs`）
- `server/contract.lua`（`MRD9.Contract` CRUD）
- `server/stats.lua`（`MRD9.Stats`・履歴・ランキング）
- `server/main.lua`（oxmysql 接続確認・スキーマ WARN・Debug コマンド `m9_sign_me` / `m9_check_contract` / `m9_my_stats`）
- `README.md`（前提条件・oxmysql・デバッグコマンド）
- `docs/FORMAL_POLICIES.md`（INSTRUCTION-006 実装済み追記・fxmanifest 補足）
- `docs/milestones.md` / `docs/design.md`（M1 完了反映）

### 完了内容

- 指示書の SQL・API 概形を実装。`Suspend` / `Terminate` の `notes` 連結は SQL 内 `\n` ではなく Lua で行末を組み立ててから `CONCAT`（可搬性のため）。
- `server_scripts` で **`contract` / `stats` を `main` より先**に読み込み。

### 実装判断（FORMAL_POLICIES 事後記録）

- 接続確認は **`MySQL.ready` ではなく** `Wait(2000)` ＋ `SELECT 1`（リポ内他 MOD に合わせた単純化）。
- `chat:addMessage` は Debug 用のみ。

### 未完了 / 次にやること

- ~~INSTRUCTION-007~~ → 本エントリで完了
- INSTRUCTION-008 以降
- 実機: `install.sql` 適用後の schema check OK / Debug コマンド・セッション転送の動作確認

### 参照

- INSTRUCTION-006 正式指示書（マスター提示）
- `docs/FORMAL_POLICIES.md`
- Commit: 実装一式 **87d33a0**（日記のコミット表記のみ **2b89e28** で追従）

---

## 2026-05-15 — INSTRUCTION-007 実行（セッション・バケットプール）

### 変更ファイル

- `config.lua`（`Config.Mission` 拡張：`bucketEnd`・同時セッション上限・周期・spawn/return 暫定座標）
- `fxmanifest.lua`（`server/session.lua` を `main.lua` より前に読み込み）
- `server/session.lua`（本実装：`MRD9.Session`・プール・タイムアウト・`playerDropped` / `onResourceStop`）
- `server/main.lua`（`/m9_test_session` / `m9_test_extract` / `m9_list_sessions`）
- `client/main.lua`（`jp-meridian9:onMissionStart` / `onMissionEnd`、`MRD9.CurrentSession`）
- `docs/CREDITS.md`（新規）
- `.gitignore`（`jp-meridian9/docs/` を追跡対象に例外追加）

### 完了内容

- D1〜D3 はマスター推奨どおり（独自実装・プール・メモリのみ＋リソース停止時の全破棄）。
- `spawnPoint` は README 注意どおり **Sandy Shores 近郊の暫定**（The Apocalypse Project の本番座標は M5 以降タスク）。
- `RemovePlayer` で離脱者に `onMissionEnd` を送る（ソロ脱出で `Destroy` が空メンバーでもクライアント状態が残らないように）。

### 未完了 / 次にやること

- INSTRUCTION-008: 契約キャッシュ・運営コマンド（**後続エントリで完了**）
- INSTRUCTION-009（ヴェガ NPC・対話）
- 実機: OneSync 環境でバケット転送・`m9_list_sessions`（コンソール）確認

### 参照

- INSTRUCTION-007 正式指示書（マスター提示）
- `docs/FORMAL_POLICIES.md` / `docs/CREDITS.md`
- Commit: **96928ab**

---

## 2026-05-15 — INSTRUCTION-008 実行（契約キャッシュ・運営コマンド）

### 確定（D4〜D6）

- **D4**: サーバー側 `contractCache`＋`LoadCache` / `UnloadCache`＋`IsContracted` の DB フォールバック。`playerJoining`（`CreateThread`+`Wait(1000)`）、`playerDropped`、`CreateThread` で既接続プレイヤー一括ウォームアップ。
- **D5**: ACE 名 **`jp-meridian9.admin`**（`Config.Admin.aceName`）。
- **D6**: `/m9_admin_sign` `/m9_admin_suspend` `/m9_admin_terminate` `/m9_admin_check` `/m9_admin_list`。

### 変更ファイル

- `server/contract.lua`（キャッシュ・参加/切断フック・起動時ウォームアップ）
- `server/main.lua`（運営コマンド 5 種・`HasAdminAce` / `notifyAdmin`）
- `config.lua`（`Config.Admin`）
- `README.md`（運営 ACE 設定例・コマンド表・概要）
- `docs/FORMAL_POLICIES.md`（`.gitignore` / `SetPlayerRoutingBucket` 公式準拠・INSTRUCTION-008）
- `docs/milestones.md` / `docs/design.md`
- 本ファイル

### 完了内容

- 運営コマンドは ACE 不成立時 **無反応**（`HasAdminAce` で早期 return）。
- `m9_admin_list` の `LIMIT` は `Config.Admin.contractListLimit`。

### 未完了 / 次にやること

- ~~INSTRUCTION-009~~ → 同日後続エントリ「INSTRUCTION-009」で完了
- INSTRUCTION-010: パーティ編成とセッション接続
- 実機: ACE 付与後の `/m9_admin_list active`、未付与時の無反応、`Config.Debug` 時の `Cache loaded` ログ

### 参照

- INSTRUCTION-008 正式指示書（マスター提示）
- `docs/FORMAL_POLICIES.md`
- Commit: **786f54a**

---

## 2026-05-15 — INSTRUCTION-009（ヴェガ NPC・対話）＋ `image/` 追跡

### 決定（D7〜D9）

- **D7**: 対話は **ox_lib**（`lib.notify` + `lib.registerContext` / `lib.showContext`）。NUI 本格化は INSTRUCTION-014/015。
- **D8**: NPC 操作は **ox_target**（`addLocalEntity` / `removeLocalEntity`）。
- **D9**: NPC 座標は暫定 `vector4(427.5, -979.3, 30.7, 90.0)`。`IsContracted` は NPC 側で追加 `LoadCache` 不要（008 のキャッシュ＋DB フォールバック）。

### 変更ファイル

- `fxmanifest.lua`（`@ox_lib/init.lua`、`dependencies` に `ox_lib` / `ox_target`）
- `config.lua`（`Config.NPC` 拡張：座標・`targetDistance`・`blockEvents`・`blip`）
- `locales/ja.lua`（ヴェガ台詞全文・`vega_flavor` 配列・パーティメニュー WIP 文言）
- `client/npc.lua` / `client/dialogue.lua`（本実装）
- `client/main.lua`（`jp-meridian9:client:openPartyMenu` スタブ）
- `server/main.lua`（`lib.callback.register`：`isContracted` / `signContract`）
- `README.md` / `docs/FORMAL_POLICIES.md` / `docs/milestones.md` / `docs/design.md`
- `jp-meridian9/image/*.jpg`（9 ファイル・復元用バックアップとして Git 追跡）
- 本ファイル

### 完了内容

- 契約締結・チュートリアル・リピートメニュー（売却は WIP 文言）、フレーバー 10 種ランダム。
- `FORMAL_POLICIES` に ox_lib / ox_target を **フレームワークではないライブラリ依存**として明記。

### 未完了 / 次にやること

- ~~INSTRUCTION-010~~ → 同日後続エントリ「INSTRUCTION-010」で完了
- INSTRUCTION-011: ゾンビアリーナ等
- 実機: `ensure` 順序（oxmysql → ox_lib → ox_target → jp-meridian9）、警察署付近 NPC・ターゲット・初回／リピート分岐

### 参照

- INSTRUCTION-009 正式指示書（マスター提示）
- Commit: **ae6d4e7**

---

## 2026-05-15 — INSTRUCTION-010（パーティ編成・ゲート〜セッション）

### 決定・逸脱

- `Session.Create` は **`{ leader = number, members = number[] }`**（`members[1]` = リーダー、全員分ループ検証）。指示書の可変長引数ではない。
- `playerDropped` のパーティ処理は **`server/session.lua` 先頭**に統合（`main.lua` 追記ではなく。切断順序の一本化）。
- 招待タイムアウトは **`SetTimeout` ではなく `CreateThread` + `Wait`**（環境差吸収）。
- ゲート確定時、**未処理招待がある場合は `Confirm` 拒否**（`err_pending_invites`）。

### INSTRUCTION-011 所感

- **ゾンビアリーナ（ループ・スポーン検証）を先**にすると、既存 `Session` / バケット上で戦闘負荷と同期を切り分けやすい。**マップ統合（The Apocalypse 等）**はスポーン・Navmesh が固まってからのほうが座標の手戻りが少ない。

### 変更ファイル

- `config.lua`（`Config.Party` 拡張、`Config.NPC.blip` 既定オン・sprite 498・`labelKey`）
- `fxmanifest.lua`（`server/party.lua` を `server/main.lua` の前へ）
- `server/session.lua`（`playerDropped` で `Party.HandleDisconnect` / `Destroy` で `NotifySessionDestroyed`）
- `server/party.lua`（本実装・`lib.callback`・Debug `/m9_party_*`）
- `client/party.lua` / `client/dialogue.lua`（`openGateSubmenu`）/ `client/npc.lua` / `client/main.lua`（パーティ stub 削除）
- `locales/ja.lua` / `README.md` / `docs/FORMAL_POLICIES.md` / `docs/milestones.md` / `docs/design.md`
- 本ファイル

### 完了内容

- 招待・承諾／拒否・タイムアウト・キック・解散・リーダー自動譲渡（編成中のみ）。
- 編成確定 → `Session.Create` → `TransferIn`。失敗時 `forming` ロールバック。

### 未完了 / 次にやること

- INSTRUCTION-011 以降（ゾンビ・ルート等）
- 実機: 2 人招待〜確定、`/m9_list_sessions` でバケット一致確認

### 参照

- INSTRUCTION-010 正式指示書（マスター提示）
- Commit: **d2c3746**

---

## 2026-05-15 — INSTRUCTION-011（ゾンビアリーナ・スポーン／ウェーブ／全滅送還）

### 決定・逸脱

- 失敗時: `arenaMissionFailed`（`lib.notify`）→ `Session.Destroy(..., 'arena_wiped')` → `onMissionEnd` で HP1 + `SetPedToRagdoll`（FORMAL_POLICIES 記載どおり）。
- `Session.Destroy` 冒頭で `MRD9.Arena.Cleanup`（掃除イベント→送還の順）。
- ゾンビ生成は **リーダーのみ** `spawnZombie`（OneSync 下の同期は未検証・実機で要確認）。

### 変更ファイル

- `config.lua`（`Config.Arena` 末尾追記）
- `fxmanifest.lua`（`server/arena/*` / `client/arena.lua`）
- `server/session.lua`（`TransferIn` で `Arena.Start`、`Destroy` 冒頭で `Arena.Cleanup`）
- `server/arena/arena.lua`（新規・状態／ウェーブ／デバッグコマンド）
- `client/arena.lua`（新規）/ `client/main.lua`（`onMissionEnd` ノックダウン・`ClientBeginMission` 呼び出し）
- `locales/ja.lua` / `docs/FORMAL_POLICIES.md` / `docs/milestones.md` / `docs/design.md`
- 本ファイル

### 未完了 / 次にやること

- INSTRUCTION-013 以降。アリーナ実機: マルチプレイでゾンビ同期・撃破カウントの検証。

### 参照

- INSTRUCTION-011 確定稿（チャット）
- Commit: **1e65df8**

---

## 2026-05-15 — INSTRUCTION-012（ルート取得・ox_target / callback）

### 決定・逸脱

- 配置: `LootSpawns` が有効ならそこから、無ければ `spawnPoint` 半径内ランダム（設計 Q4）。
- 取得: `lib.callback` のみ（ネットイベント直送禁止方針に沿う）。
- `fxmanifest` で `server/loot.lua` を `session.lua` の直後に移動（読み込み順の明確化）。

### 変更ファイル

- `config.lua`（`Config.Loot` / `Config.LootRarityWeight`）
- `server/loot.lua` / `client/loot.lua`（本実装）
- `server/session.lua`（`Loot.Spawn` / `Loot.Cleanup`）
- `fxmanifest.lua` / `locales/ja.lua` / `docs/FORMAL_POLICIES.md` / `docs/milestones.md` / `docs/design.md`
- 本ファイル

### 未完了 / 次にやること

- INSTRUCTION-013（脱出）。ルート実機: リーダー以外の `addEntity` 同期・`PlaceObjectOnGroundProperly` 後のネット ID。

### 参照

- 設計確定（チャット・INSTRUCTION-012）
- Commit: **b47e1a2**

---

## 2026-05-15 — INSTRUCTION-013（脱出・progressCircle / 個別離脱 / 記録）

### 決定・逸脱

- 個別離脱を採用（Q1=a, Q2=α, Q3=A, Q4=i）。
- Destroy 冒頭で `MRD9.Extract.OnSessionDestroy` を呼び、未脱出メンバーを `outcome` 別にログ。
- `Config.ExtractPoints` の暫定 3 箇所は `spawnPoint`（1972, 3818, 33.4）周辺の北・南・東に手置き。

### 変更ファイル

- `config.lua`（`Config.Extract` / `Config.ExtractPoints` を末尾に。旧空テーブル定義はコメント化）
- `server/extract.lua`（本実装・`lib.callback` / mission_logs / Stats.Update）
- `client/extract.lua`（本実装・ブリップ・近接ループ・`lib.progressCircle`・キャンセル監視）
- `server/session.lua`（`Destroy` 冒頭で `Extract.OnSessionDestroy`）
- `fxmanifest.lua`（`server/loot.lua` / `server/extract.lua` の重複削除）
- `locales/ja.lua` / `docs/FORMAL_POLICIES.md` / `docs/milestones.md` / `docs/design.md`
- 本ファイル

### 未完了 / 次にやること

- INSTRUCTION-014（HUD/NUI）。脱出実機: アニメ `random@arrests` の挙動・複数同時脱出・mission_logs `items_recovered_json` 内容の確認。

### 参照

- 設計確定（チャット・INSTRUCTION-013）
- Commit: **0cce60d**

---

## 2026-05-15 — INSTRUCTION-014（HUD/NUI）設計書作成

### 決定・逸脱

- 以後の INSTRUCTION は **コーディング着手前に `jp-meridian9/docs/INSTRUCTION-XXX（...）.md` の設計書を独立ファイルで作成** する運用に変更（マスター指示）。
- 設計書ファイル名は Windows 制約で `/` が使えないため **全角中点 `・`** で置換（例: `INSTRUCTION-014（HUD・NUI）.md`）。
- 014 では未確定 Q（Q1〜Q6）を設計書内に列挙し、マスター回答後に着手する運用。

### 変更ファイル

- `jp-meridian9/docs/INSTRUCTION-014（HUD・NUI）.md`（新規・設計書のみ）
- 本ファイル

### 未完了 / 次にやること

- 設計書 §3 の Q1〜Q6 にマスター回答を反映 → コーディング AI に渡す。
- 014 実装後に `FORMAL_POLICIES.md` / `milestones.md` / `design.md` を更新。

### 参照

- 設計書: `jp-meridian9/docs/INSTRUCTION-014（HUD・NUI）.md`
- Commit: **1434aef**

---

## 2026-05-15 — INSTRUCTION-014（HUD/NUI）実装

### 決定・逸脱

- Q1=(a) サーバー `server/hud.lua` 集約、`jp-meridian9:client:hud:state` 配信。Q2=(α) 総数+レアリティ。Q3=(iii) パーティ先頭が自分（数値のみ・バーなし）。Q4=(A) ウェーブ帯。Q5=(I) 脱出は `lib.showTextUI` のみ（HUD 脱出バッジなし）。Q6=既定周期。
- `fxmanifest`: `server/hud.lua` は `arena.lua` 直後。`client/hud.lua` は `main.lua` より前。
- NUI トーストは `#app` 外の `#m9-toasts` に出し、脱出成功トーストが `m9_hud_hide` 直後も短時間表示できるようにした。

### 変更ファイル

- `server/hud.lua` / `client/hud.lua` / `client/main.lua` / `client/arena.lua`
- `html/index.html` / `html/style.css` / `html/app.js`
- `fxmanifest.lua` / `locales/ja.lua`（HUD キーは既存ブロックを利用）
- `docs/INSTRUCTION-014（HUD・NUI）.md` / `docs/FORMAL_POLICIES.md` / `docs/milestones.md` / `docs/design.md`
- 本ファイル

### 未完了 / 次にやること

- 実機: `resmon`・ルート取得直後のインベントリカウンタ・リソース `restart` 時の NUI 状態。

### 参照

- Commit: **4acf1a4**

---

## 2026-05-15 — HUD UI スケール 2 倍化（INSTRUCTION-014 追補）

### 決定・逸脱

- 実機で文字が小さいというマスター指摘を受け、CSS 変数 `--m9-scale`（既定 2.0）で **HUD 全体のフォント・余白・最小幅** を一括スケールする方式を採用。
- 倍率は `Config.HUD.uiScale` に保持し、`m9_hud_locale` の `payload.uiScale` で NUI に渡す。NUI 側で `document.documentElement.style.setProperty('--m9-scale', ...)`。
- `#app` への `transform: scale()` は **採用しない**（オーバーレイ位置がはみ出すため）。
- `mrd9_contracts` テーブル未作成（起動時の `[WARN] mrd9_contracts not found` 警告）はマスター側で `sql/install.sql` を `fivem_db` に適用して解消する運用作業として案内。コード変更なし。

### 変更ファイル

- `html/style.css`（CSS 変数化）
- `html/app.js`（`applyLocale` で `--m9-scale` を反映）
- `client/hud.lua`（`buildLocalePayload` に `uiScale` を同梱）
- `config.lua`（`Config.HUD.uiScale = 2.0` 追記）
- 本ファイル

### 未完了 / 次にやること

- 実機で 2 倍が大きすぎる場合は `Config.HUD.uiScale` を 1.5 程度に下げる。
- マスターによる `sql/install.sql` の DB 適用 → `restart jp-meridian9` で警告消去・契約成功確認。

### 参照

- Commit: **22b3caf**

---

## 2026-05-15 — `sql/install.sql` 自動適用（起動時スキーマ生成）

### 決定・逸脱

- 起動時に `mrd9_contracts` が存在しないときの挙動を、警告 `print` から **自動適用** に変更（マスター指示）。
- スキーマの単一情報源は **`sql/install.sql`** のまま。`LoadResourceFile(... , 'sql/install.sql')` で読み込み、Lua 側で `--` 行コメントを剥がし、`;` で分割して `MySQL.query.await` で順次実行。
- 採用しなかった案: Lua にスキーマを文字列定数として埋め込む（二重定義になりメンテ事故の温床）。
- 失敗時は **手動適用にフォールバック**（`[ERROR]` を `print` して処理続行。プレイヤー操作が来るまで時間があるためサーバー全停止は避ける）。
- 既知の制約: パーサは `--` を行コメントとして単純に除去するため、将来 `COMMENT='--…'` のような **文字列リテラル内に `--` を含むスキーマ** を追加するなら、正規 SQL トークナイザーに差し替える必要がある。現状の `MERIDIAN-9 契約者管理` 等は単一ハイフンなので問題なし（`FORMAL_POLICIES.md` に注意書きを追加）。

### 変更ファイル

- `server/main.lua`（`splitSqlStatements` / `autoInstallSchema` / `mrd9ContractsExists` 追加。起動スレッドで分岐）
- `docs/FORMAL_POLICIES.md`（INSTRUCTION-006 表に「スキーマ自動適用」行を追記）
- 本ファイル

### 未完了 / 次にやること

- 実機で **`restart jp-meridian9`** → ログに `auto-install: applying 3 SQL statements ...` と `schema auto-install OK (mrd9_contracts created)` が出ることを確認。
- ヴェガと契約 → 「契約処理に失敗しました」が消えることを確認。

### 参照

- Commit: **98224b8**

---

## 2026-05-15 — サーバー側クライアント専用ネイティブ誤用の修正

### 決定・逸脱

- ソロ任務開始時に F8 / サーバーコンソールに 3 件の `nil value (global ...)` エラーが出ていた。
  - `server/loot.lua:102` — `GetGroundZFor_3dCoord`（クライアント専用）
  - `server/arena/spawn.lua:48` — `GetGroundZFor_3dCoord`（同上）
  - `server/hud.lua:64` — `IsPedDeadOrDying`（サーバー API に存在しない）
- 修正方針:
  - loot は **クライアント側で `PlaceObjectOnGroundProperly(obj)`** を既に呼んでいる（`client/loot.lua:96`）ため、サーバー側の地面 Z 補正は **そもそも不要**。center.z + 0.12 を素直に渡す。
  - arena は **`client/arena.lua` の `spawnZombie` ハンドラに `GetGroundZFor_3dCoord` を移動**。サーバー `PickCoordsNearPlayer` は半径ランダム化だけして `c.z + 0.5` を返す。
  - HUD の生存判定は **`hp > 0`** で代替（サーバー API に `IsPedDeadOrDying` は存在しないため）。
- 関連する二次症状: 「ソロ→どこかの家の中で出れない」は、上記エラーで `lib.callback` が失敗 → 進行が刺さる連鎖の可能性。本修正で先に再検証する。

### 変更ファイル

- `server/loot.lua` / `server/arena/spawn.lua` / `server/hud.lua`
- `client/arena.lua`（`spawnZombie` に地面 Z 補正を追加）
- 本ファイル

### 未完了 / 次にやること

- 実機で `restart jp-meridian9` → ソロ任務開始 → 上記 3 エラーが出ないこと、移動できることを確認。
- 残課題（**Phase 2**）: ヴェガ対話を **独自 NUI ウィンドウ化**＋HUD と同じスケール（`--m9-scale`）に統一。`lib.notify` / `lib.alertDialog` のままだと ox_lib 側の CSS が優先されてフォントが伸びない。設計書追補→マスター確認後に着手。

### 参照

- Commit: **733bd55**

---

## 2026-05-15 — mrd9_mission_logs `started_at` NULL エラー修正

### 決定・逸脱

- 脱出 → `Stats.LogMission` で `Column 'started_at' cannot be null` が発生。
- 根本原因:
  - `server/session.lua` の `Create` で `startedAt = GetGameTimer()`（ミリ秒整数）を保持していたが、DB は DATETIME 文字列を要求する。
  - `server/extract.lua` の `logMissionFor` は `startedAt = nil` をハードコードで渡していた。
- 修正:
  1. **session に `startedAtIso = os.date('%Y-%m-%d %H:%M:%S')` を追加**。既存の `startedAt`（ミリ秒）は経過時間計算用に温存。
  2. `logMissionFor` で **`startedAt = session.startedAtIso`** を渡す。
  3. **`Stats.LogMission` 側に NOT NULL ガード**（呼び出し漏れでも `os.date(...)` で fallback）。

### 変更ファイル

- `server/session.lua` / `server/extract.lua` / `server/stats.lua`
- 本ファイル

### 未完了 / 次にやること

- 実機で脱出 → `mrd9_mission_logs` に `started_at = <作成時刻>`, `ended_at = <脱出時刻>` が記録されるか確認。
- Phase 2（対話 NUI ウィンドウ化＋スケール統一）はマスター回答待ち。

### 参照

- Commit: **9f53925**

---

## 2026-05-15 — returnPoint 修正＋SiteNine 演出の最小適用

### 決定・逸脱

- 脱出後の帰還座標が `Maze Bank Tower 最上階 (Z=326.18)` の暫定値で残っており、「タワーの奥所に転送→降りられない」状態だった。
  - **`Config.Mission.returnPoint`** を `vector4(425.0, -979.3, 30.5, 270.0)`（ヴェガ事務所 NPC の隣、対面 heading）に変更。
- `Config.SiteNine`（天候 XMAS・深夜 3 時・時間停止）は **定義のみで参照ゼロ**だった。これが「現実世界でゾンビが出るだけ」の正体。
- 最小実装で適用：
  - `client/transition.lua` に `MRD9.Transition.Enter()` / `Leave()` を実装。
  - `Enter()`: `NetworkOverrideClockTime` / `SetWeatherTypeNowPersist` / `SetWeatherTypeNow` / `SetOverrideWeather` / `SetTimecycleModifier('spectator5', 1.0)` / `SetArtificialLightsState(true)`。
  - `Leave()`: 上記をすべて解除。`onResourceStop` でも `Leave()` を呼ぶ。
  - `Config.SiteNine` に `timecycleModifier` / `timecycleStrength` / `blackout` / `timeMinute` を追加。
  - `client/main.lua` の `onMissionStart`/`onMissionEnd` で `Transition.Enter/Leave` を呼ぶ（HUD と並列）。
- スコープ外（次の INSTRUCTION で対応）:
  - ゲート転送演出（フェード・SFX・カメラワーク）
  - Site Nine 専用ジオメトリ／オーバーレイ／別 MAP
  - 入場時の説明テキスト、霧パーティクル、Subject-0 演出  
  → INSTRUCTION-017 の設計書でまとめて扱う。

### 変更ファイル

- `config.lua`（`returnPoint`／`SiteNine` 拡張）
- `client/transition.lua`（スタブ→本実装）
- `client/main.lua`（`Transition.Enter`/`Leave` 呼び出し）
- 本ファイル

### 未完了 / 次にやること

- 実機: 任務開始で **雪・深夜・寒色フィルター・街灯消灯** が効くこと。脱出後にすべて解除されること。脱出帰還がヴェガ事務所の地面になること。
- INSTRUCTION-017（ゲート演出・没入感強化）の設計書を別途立てる。

### 参照

- Commit: **fa16f13**

---

## 2026-05-15 — INSTRUCTION-020（サイト・ナイン MAP 導入）設計書作成

### 決定・逸脱

- マスターから当初設計書（`全体設計.txt`）全文を再提示され、**M0 環境構築で「The Apocalypse Project 導入」が抜け落ちていた**ことを正式に認識。
- 当初提示した「(α) MAP 無し / (γ) IPL 切替」案は**撤回**。設計書通り The Apocalypse Project ([Arcainex/The-Apocalypse-Project](https://github.com/Arcainex/The-Apocalypse-Project)) を採用。
- マスター指示「方式 (I) bucket 内だけ荒廃」の実装可否を GitHub API でリポ調査 → **技術的に不可能**と判明。
  - routing bucket は entity 同期のみ、ymap には適用不可
  - 本 MAP は `_manifest.ymf` ベースで起動時にグローバル配信、`RequestIpl/RemoveIpl` 制御外
  - クライアント別 ymap 状態は持てない
- 現実解として **(IV) 部分採用** を推奨：
  - ヴェガ事務所（Mission Row）に影響する `[DestroyedBldg]` `casino` `paleto` を除外
  - 採用は `Factory` / `[CountyUpgrade]` / `[Mine]` / `assets_map2/*` / `assets_map3/*` / `postapo-interior` 等
  - RP 設定補正: 「**サイト・ナイン＝ロスサントス北部の隔離区域**」「**次元の歪みでこの区域だけ別次元から漏れ出した**」
- 本リポへは **同梱しない**（LICENSE 不明・再配布リスク回避）。運営者は GitHub から直接 clone。
- 設計書を `docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md` に保存。実装はマスター承認後。

### 変更ファイル

- `docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md`（新規・設計書のみ）
- 本ファイル

### 未完了 / 次にやること

- マスターから採用案 (IV) と除外フォルダリストへの承認
- 承認後: テストサーバー `resources/[maps]/apocalypse_project/` へ clone、不要フォルダ削除、実機で衝突確認 → `Config.Mission.spawnPoint` を MAP 内に更新

### 参照

- 設計書: `jp-meridian9/docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md`
- 上流リポ: [https://github.com/Arcainex/The-Apocalypse-Project](https://github.com/Arcainex/The-Apocalypse-Project)
- Commit: **b546412**

---

## 2026-05-15 — INSTRUCTION-020 全面書き直し（北ヤンクトン版・v2）＋ サーバーパス訂正

### 決定・逸脱

- マスター指摘「LS 内ジオメトリ置換は車・ヘリで地続きで行ける、海の向こうならいい」を受け、**The Apocalypse Project 採用は完全撤回**。
- 採用は **GTA V バニラ同梱の North Yankton**＋IPL ローダー [Bob74/bob74_ipl](https://github.com/Bob74/bob74_ipl)（MIT、現役）。
- 技術的決定打：`NorthYankton.Enable(true/false)` は **クライアントローカルネイティブ**で、routing bucket と組み合わせると **真の「bucket 内分離」**が成立。bucket 0 のプレイヤーには **海面のまま**、任務 bucket のメンバーだけ北ヤンクトンが見える。
- LICENSE: MIT（bob74_ipl）＋ バニラ GTA V リソース → 同梱問題なし。
- 世界観: 雪・教会・墓地・無人の港町 → 既存 `Config.SiteNine` (XMAS / 深夜 3 時 / 寒色 / 街灯消灯) と完全に整合。ストーリー §「インシデント・ゼロ」「Subject-0」「研究員失踪」のホラー演出強化。
- `alberttheprince/NorthYankton`（同等機能のリソース）は参考実装として把握、本実装では採用せず（jp-meridian9 セッション制御に組み込む方が整合）。
- **サーバーデプロイ先パス**: マスターの実環境は `H:\CURSOR\FiveMServer\txData\FiveMBasicServerCFXDefault_EC2B5A.base\resources\[jp-mods]\` で、`AGENTS.md` / `.cursor/rules/cursor-workflow.mdc` / `scripts/deploy-all.bat` の `C:\FiveMServer\server-data\...` は **誤情報**。同時に訂正。
- 設計書 `docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md` を **v1（apocalypse 案）から v2（北ヤンクトン案）に全面書き直し**。実装はマスター承認後。

### 変更ファイル

- `jp-meridian9/docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md`（v2 に全面書き直し）
- `AGENTS.md`（テストサーバーパス訂正）
- `.cursor/rules/cursor-workflow.mdc`（テストサーバーパス訂正）
- `scripts/deploy-all.bat`（DEV_ROOT / SERVER_ROOT 訂正）
- `.gitignore`（`jp-meridian9/map/` を除外）
- `jp-meridian9/map/apocalypse_project/`（clone 物を削除、map/ ごと削除）
- 本ファイル

### 未完了 / 次にやること

- マスターから設計書 v2 への承認
- 承認後の実装手順（INSTRUCTION-020 §7）:
  1. テストサーバーへ `bob74_ipl` を `git clone`
  2. `server.cfg` に `ensure bob74_ipl` 追加
  3. `fxmanifest.lua` `dependencies` に追記
  4. `client/transition.lua` に `applyNorthYankton/clearNorthYankton` 実装
  5. `Config.Mission.spawnPoint` を `(3217.697, -4834.826, 113.0, 90.0)` に
  6. `Config.ExtractPoints` / `Config.LootSpawns` を北ヤンクトン内座標に再配置
  7. 実機テスト（ソロ＋ bucket 分離テスト）
  8. ドキュメント整備（README / CREDITS / FORMAL_POLICIES / milestones / design）

### 参照

- 設計書: `jp-meridian9/docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md`
- bob74_ipl: [https://github.com/Bob74/bob74_ipl](https://github.com/Bob74/bob74_ipl)
- bob74_ipl Wiki: North Yankton: [https://github.com/Bob74/bob74_ipl/wiki/GTA-V:-North-Yankton](https://github.com/Bob74/bob74_ipl/wiki/GTA-V:-North-Yankton)
- alberttheprince/NorthYankton（参考）: [https://github.com/alberttheprince/NorthYankton](https://github.com/alberttheprince/NorthYankton)
- Commit: **3b2fe86**

---

## 2026-05-15 — INSTRUCTION-020 v2 実装（北ヤンクトン / bob74_ipl 連携）

### 決定・逸脱

- マスター承認を受けて INSTRUCTION-020 v2 を実装。MAP 戦略は **GTA V バニラ同梱の North Yankton ＋ bob74_ipl** で確定。
- テストサーバーへ `bob74_ipl` を `H:\CURSOR\FiveMServer\txData\FiveMBasicServerCFXDefault_EC2B5A.base\resources\[gamemodes]\[maps]\bob74_ipl\` に `git clone --depth 1`。
- `server.cfg` の Admin ブロック直後（JP Mods より前）に **`ensure bob74_ipl`** を追記。
- `jp-meridian9` 側の主要変更:
  - **`fxmanifest.lua`**: `dependencies` に `'bob74_ipl'` 追加
  - **`client/transition.lua`**: `applyNorthYankton` / `clearNorthYankton` 実装。`Transition.Enter` で `NorthYankton.Enable(true)` + `Grave.Set(dug)` + `Traffic.Enable(false)` → `Wait(iplLoadWaitMs)` → 既存 SiteNine 演出を適用。`Transition.Leave` で逆順
  - **`server/session.lua`**: `TransferIn` を **2 段テレポート**化（メンバーへ `onMissionStart` 送信 → IPL ロード待ち `Wait(2000)` → `SetEntityCoords`）。IPL ロード完了前のテレポートで地形貫通する事故を防止
  - **`server/main.lua`**: 起動チェック `GetResourceState('bob74_ipl')` を `CreateThread`+`Wait(3000)` で実行
  - **`config.lua`**:
    - `Config.Mission.spawnPoint = vector4(3217.697, -4834.826, 113.0, 90.0)`（Ludendorff 中心、東向き）
    - `Config.Mission.siteNineLoadWaitMs = 2000`（サーバー側テレポート前の追加待機）
    - `Config.SiteNine` に `northYankton=true` / `graveStyle='dug'` / `traffic=false` / `iplLoadWaitMs=2000` 追加
    - `Config.LootSpawns` を **北ヤンクトン内 15 箇所**で再定義（墓地・銀行は高レア寄り）
    - `Config.ExtractPoints` を **教会前広場 / 駅プラットフォーム / 墓地裏門** の 3 箇所に再定義
- `scripts/deploy.bat` を **英語化＋CRLF** に書き直し（旧版は LF＋日本語 Shift-JIS で PowerShell 経由実行で破損していた）。`scripts/deploy.bat jp-meridian9` でテストサーバーへ反映成功（64 ファイル）。
- ドキュメント整備:
  - `README.md`: 「サイト・ナイン MAP 導入手順」セクション新設。`Config.SiteNine` 表を整備。`bob74_ipl` 必須化を明記
  - `docs/CREDITS.md`: Bob74/bob74_ipl（MIT）を採用節に追加。Arcainex/The Apocalypse Project を「検討したが採用しなかった案」に移動
  - `docs/FORMAL_POLICIES.md`: INSTRUCTION-020 正本セクション追加（北ヤンクトン版 v2 / bob74_ipl / 分離方式 / 撤回案）
  - `docs/milestones.md`: M0 に INSTRUCTION-020 行を追加（完了）
  - `docs/design.md`: 「サイト・ナイン MAP」現状節に北ヤンクトン採用を追記

### 変更ファイル

- `jp-meridian9/fxmanifest.lua` / `jp-meridian9/config.lua` / `jp-meridian9/client/transition.lua` / `jp-meridian9/server/session.lua` / `jp-meridian9/server/main.lua`
- `jp-meridian9/README.md` / `jp-meridian9/docs/CREDITS.md` / `jp-meridian9/docs/FORMAL_POLICIES.md` / `jp-meridian9/docs/milestones.md` / `jp-meridian9/docs/design.md`
- `scripts/deploy.bat`（英語化・CRLF 化）
- `H:\CURSOR\FiveMServer\txData\FiveMBasicServerCFXDefault_EC2B5A.base\server.cfg`（`ensure bob74_ipl` 追記、リポ外）
- `H:\CURSOR\FiveMServer\txData\FiveMBasicServerCFXDefault_EC2B5A.base\resources\[gamemodes]\[maps]\bob74_ipl\`（clone、リポ外）
- 本ファイル

### 未完了 / 次にやること

- **実機テスト**（マスター実施）:
  - txAdmin で `restart bob74_ipl; restart jp-meridian9` または再起動
  - 起動ログに `bob74_ipl 起動確認 OK（サイト・ナイン = 北ヤンクトン）` が出るか
  - ヴェガ契約 → ゲート起動 → **北ヤンクトン Ludendorff 中央広場にテレポート**
  - 北ヤンクトン内で雪・寒色フィルター・街灯消灯が効くか
  - 任務終了でヴェガ事務所（Mission Row）へ帰還、**綺麗な街並みのままか**
  - bucket 分離テスト（任務中プレイヤーが見ている景色が、bucket 0 のプレイヤーには見えないか）
- ExtractPoints / LootSpawns の **座標を実機で精査**して確定（暫定値）
- INSTRUCTION-017（ゲート転送演出）の設計書はマスター承認後に別途立ち上げ

### 参照

- 設計書: `jp-meridian9/docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md`
- bob74_ipl: [https://github.com/Bob74/bob74_ipl](https://github.com/Bob74/bob74_ipl)
- Commit: **b0c954d**

---

## 2026-05-15 — 北ヤンクトン転送の安全テレポート実装（落下事故修正）

### 決定・逸脱

- 実機テスト報告: 「物凄い高度の空の上から落下、途中で空母が見えた、ヴェガの目の前にワープして即死」。原因は **IPL ロード完了前のテレポートで地形コリジョン未確定**。
- 「空母が見えた」= NorthYankton.Enable(true) が間に合わず Cayo Perico（同座標重複）が一瞬見えた可能性。
- 「ヴェガの目の前にワープ」= プレイヤーが座標範囲外（高高度落下）になり FiveM のセーフティ機構が安全座標へリセット。
- **`Wait(2000)` 程度では北ヤンクトンの IPL 群（20+）がロード完了しない**。`NewLoadSceneStart` + コリジョン待ち + フェード遮蔽でクライアント側完全制御に切り替え。
- 設計変更:
  - **テレポートをクライアント側に完全委譲**。サーバー側 `TransferIn` / `RemovePlayer` / `Destroy` の `SetEntityCoords` を撤去。
  - **`MRD9.Transition.TeleportToSiteNine(sp)`** を新設:
    1. `DoScreenFadeOut(500)` + `IsScreenFadedOut` 待機
    2. プレイヤー Ped を `SetPlayerControl(false)` + `SetEntityVisible(false)` + `FreezeEntityPosition(true)` + `SetEntityCollision(false)` + `SetEntityInvincible(true)`
    3. `RequestCollisionAtCoord` + **`NewLoadSceneStart`** （最大 12 秒待機）でシーン同期ロード
    4. `SetEntityCoords` でテレポート
    5. `HasCollisionLoadedAroundEntity` で 5 秒コリジョン待ち + `Wait(500)`
    6. 各種フラグ復元 + `DoScreenFadeIn(1000)`
  - **`MRD9.Transition.TeleportToLosSantos(rp)`** を新設（帰還側、同様のフロー + `Transition.Leave()` で IPL Disable）。
  - `Transition.Enter()` から既存の `Wait(iplLoadWaitMs)` を撤去（`NewLoadSceneStart` 側で完全ロード待ち）。
- `client/main.lua`:
  - `onMissionStart` で `Transition.Enter()` の後 `Transition.TeleportToSiteNine(data.spawnPoint)`
  - `onMissionEnd` で `CreateThread` 内に整理し、`Transition.TeleportToLosSantos(rp)` 完了後に `arena_wiped` 時の ragdoll を発火（フェード/Invincible との競合解消）
- `server/session.lua`:
  - `TransferIn`: テレポート撤去、`onMissionStart` 発火のみ
  - `RemovePlayer` / `Destroy`: テレポート撤去、`onMissionEnd` ペイロードに `returnPoint` を含めてクライアント任せ
  - bucket リセット（`setPlayerBucket(src, 0)`）は維持

### 変更ファイル

- `jp-meridian9/client/transition.lua`（`TeleportToSiteNine` / `TeleportToLosSantos` 新設）
- `jp-meridian9/client/main.lua`（`onMissionStart` / `onMissionEnd` ハンドラ再構成）
- `jp-meridian9/server/session.lua`（サーバー側 `SetEntityCoords` 撤去、`returnPoint` をイベントに同梱）
- 本ファイル

### 未完了 / 次にやること

- **実機再テスト**: ゲート起動 → フェード黒幕中に LoadScene 待機 → フェードイン後に北ヤンクトン Ludendorff 中央広場に立っている、落下なし
- 帰還: 脱出 → フェード黒幕 → ヴェガ事務所前にフェードイン
- arena_wiped 時: 帰還＋ヴェガ事務所前で ragdoll 1HP

### 参照

- Commit: **798ad78**

---

## 2026-05-15 — 北ヤンクトン IPL ロード完了待ちを `IsIplActive` で実装

### 決定・逸脱

- 実機テスト報告: 「どこかの海の上空に転送→着水→即死」。スクショで HUD は正常動作（WAVE 1/3 開始）だが、プレイヤーは **海中で何も見えない**。
- 原因: `bob74_ipl/gtav/north_yankton.lua` を実調査した結果、`NorthYankton.Enable(true)` は内部で **29 個の IPL（`prologue01〜06`, `prologuerd`, `prologue_occl`, `DES_ProTree_start` 等）を `RequestIpl` で発射するだけで非同期**。`NewLoadSceneStart` は ymap ストリーミング用で IPL ロード待ちには効かない。
- 修正: `MRD9.Transition.TeleportToSiteNine` 内で、フェード後・テレポート前に **`IsIplActive` で 29 個全てが active になるまで `Wait(50)` ループ**（タイムアウト 15 秒）。
- `waitIplsActive(iplList, timeoutMs)` を追加。`getNorthYanktonObject()` から `NY.ipl` を取って渡す。
- IPL ロード後に従来の `NewLoadSceneStart` + コリジョン待ちを実行（ymap/ybn のストリーミング促進）。

### 変更ファイル

- `jp-meridian9/client/transition.lua`（`waitIplsActive` 追加、`TeleportToSiteNine` に IPL ロード完了待ち挿入）
- 本ファイル

### 未完了 / 次にやること

- 実機再テスト: フェード黒幕が **数秒〜15秒**かかる可能性（IPL ロード時間）。フェードインしたら **北ヤンクトン Ludendorff 中央広場に立っている**こと。
- 15 秒以内に全 IPL がロードできない場合は `MRD9.Log` で WARN が出る（`Config.Debug=true` 時のみ表示）。実機で再現したらタイムアウト延長検討。

### 参照

- bob74_ipl の North Yankton IPL リスト（29 個）: `gtav/north_yankton.lua`
- `EnableIpl` 実装: `lib/common.lua`（`RequestIpl` 非同期）
- Commit: **57a2e76**

---

## 2026-05-15 — INSTRUCTION-020 を v3（Cayo Perico）に確定・実装

### 決定・逸脱

- マスター実機検証の結果、**北ヤンクトンは Prologue 専用未完成エリアで建物の横壁が欠落・雪で移動減速**。**Cayo Perico は GTA Online Heist DLC として完成度の高い熱帯島**で、戦場として最適と判断。
- INSTRUCTION-020 を **v3 Cayo Perico 版**に全面書き直し。撤回履歴: v1 The Apocalypse Project（LS 内 ymap 直配置で bucket 分離不可）→ v2 North Yankton（ジオメトリ未完成）→ **v3 Cayo Perico**。
- 採用ネイティブ: **`SetIslandEnabled('HeistIsland', true/false)`**（クライアントローカル、bob74_ipl 不要）
- 北ヤンクトン実装（`applyNorthYankton` / `m9_ny` / `Config.SiteNine.island = 'northYankton'`）は **コード上残置**（将来のホラー演出・ロアロケーション拡張用、検証期間中の比較用）
- `Config.SiteNine.island` で MAP 切替（`'cayoperico'` / `'northYankton'` / `'none'`）
- 演出変更: 雪 (XMAS) → 雷雨 (THUNDER)、深夜 3 時 → 夜 22 時、寒色 (`spectator5`) → 青み (`phone_cam11`)、街灯消灯 → なし（Cayo Perico は元から無人）
- 座標は **仮値**で `Config.Mission.spawnPoint = (4523, -4974, 4.5)` メインビーチ、`Config.ExtractPoints` 3 ヶ所、`Config.LootSpawns` 15 ヶ所。実機 `m9_cayo coords` で精査して確定する想定。
- ストーリー再解釈: 「サイト・ナイン = 次元の歪みで現実世界から切り離された熱帯島」「El Rubio 元秘密基地が異次元化、研究員 25 名はジャングル深部で失踪」「Subject-0 はメインコンパウンド地下のサブマリン基地に眠る」「夜と雷雨は次元の歪みによる異常気象」。`ストーリー.txt` は無改変で整合。
- ヴェガ事務所のスクショ撮影でマスターからオブジェクト浮遊報告（北ヤンクトンと Cayo Perico の両方が enable された状態）→ `m9_ny on/tp` と `m9_cayo on/tp` に **排他制御**追加（一方を on にしたら他方を off）。`Transition.Enter` の `applyIsland()` でも `Config.SiteNine.island` 一値で分岐するため運用上排他成立。

### 変更ファイル

- `jp-meridian9/docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md`（v3 に全面書き直し）
- `jp-meridian9/config.lua`（`Config.Mission.spawnPoint` / `Config.ExtractPoints` / `Config.LootSpawns` を Cayo Perico へ、`Config.SiteNine` を熱帯ホラー演出に、`island='cayoperico'` 追加）
- `jp-meridian9/client/transition.lua`（`applyCayoPerico` / `clearCayoPerico` / `applyIsland` / `clearIsland` 追加、`Enter/Leave/TeleportToSiteNine` を島種別で分岐）
- `jp-meridian9/client/main.lua`（`m9_cayo` / `m9_ny` の排他制御）
- `jp-meridian9/README.md` / `jp-meridian9/docs/CREDITS.md` / `jp-meridian9/docs/FORMAL_POLICIES.md` / `jp-meridian9/docs/milestones.md` / `jp-meridian9/docs/design.md` を Cayo Perico 採用に更新
- 本ファイル

### 未完了 / 次にやること

- **実機テスト**: `restart jp-meridian9` → ヴェガ依頼 → ゲート起動 → Cayo Perico メインビーチに雷雨でフェードイン → 戦闘 → 港 / 滑走路 / 丘陵から脱出 → ヴェガ事務所へ綺麗に帰還
- **座標精査**: マスターが `m9_cayo coords` で実機歩き回って取得 → `Config.Mission.spawnPoint` / `Config.ExtractPoints` / `Config.LootSpawns` を確定
- **bucket 分離テスト**: 運営 + テスト用 1 名で「任務中の運営が見ている Cayo Perico が、bucket 0 のテスト用には見えない」を確認

### 参照

- 設計書: `jp-meridian9/docs/INSTRUCTION-020（サイト・ナイン MAP 導入）.md`（v3）
- Commit: **a6c634a**

---

## 2026-05-15 — INSTRUCTION-021 オープンワールド・サバイバル + Cayo Perico 確定座標反映

### 決定・逸脱

- マスター実機で取得した Cayo Perico の座標を反映：
  - **`Config.Mission.spawnPoints`**: 住宅街 5 ヶ所からランダム選出（チーム分散しすぎ防止）
  - **`Config.ExtractPoints`**: 監視塔 / ヘリポート / テント / 飛行場 / 配電施設 の 5 ヶ所
- ゲームデザイン変更（マスター提案）: 「アリーナ単発」→ 「アリーナ序盤 + 自由探索 + 持続的脅威」のハイブリッドへ。
- **INSTRUCTION-021 オープンワールド・サバイバル**を新規設計書として起こし、実装：
  - `Config.Survival = { enabled, intervalMs=180000, countPerPlayer=3, radiusMin=30, radiusMax=150, zombieHealth=100, zombieModels }`
  - **`server/survival.lua`**: `MRD9.Survival.Start/Stop/IsActive`。`CreateThread` で `intervalMs` ごとに `MRD9.Arena.Spawn.PickCoordsNearPlayer` を流用して各メンバー周辺にスポーン
  - フック: `arena/arena.lua` `_onWaveCleared` の cleared 分岐で `Survival.Start`、`session.lua` `TransferIn` で `Config.Arena.enabled=false` 時直接 `Survival.Start`、`Destroy` で `Survival.Stop`
  - `MRD9.Arena.Spawn.PickCoordsNearPlayer` に `minROverride` / `maxROverride` 引数追加（後方互換）
  - Survival ゾンビは `arenaStates[sessionId]` に登録せず、kill count に貢献しない純粋な脅威
- `fxmanifest.lua` の `server_scripts` に `'server/survival.lua'` を `'server/arena/arena.lua'` の直後に追加
- 既存 INSTRUCTION-011（アリーナ）/-013（脱出）/-014（HUD）/-012（ルート）は **無変更**

### 変更ファイル

- `jp-meridian9/docs/INSTRUCTION-021（オープンワールド・サバイバル）.md`（新規・設計書）
- `jp-meridian9/server/survival.lua`（新規）
- `jp-meridian9/config.lua`（spawnPoints 配列化、ExtractPoints 5 ヶ所、Survival 設定）
- `jp-meridian9/server/arena/spawn.lua`（PickCoordsNearPlayer 半径オーバーライド）
- `jp-meridian9/server/arena/arena.lua`（cleared 分岐で Survival.Start フック）
- `jp-meridian9/server/session.lua`（spawnPoints ランダム選出 + Arena.enabled=false 時 Survival 直接開始 + Destroy で Stop）
- `jp-meridian9/fxmanifest.lua`（survival.lua 追加）
- `jp-meridian9/docs/FORMAL_POLICIES.md` / `docs/milestones.md` / `docs/design.md`
- 本ファイル

### 未完了 / 次にやること

- **実機テスト**: 3 ウェーブクリア後にゾンビ通知 + 3 分後に各メンバー周辺にスポーン
- **`Config.Arena.enabled = false` テスト**: TransferIn 直後から Survival 起動
- **タイムアウト・全滅・個別脱出 → Survival 停止**確認
- LootSpawns 15 ヶ所の座標精査（住宅街・港・滑走路・配電施設など島内に分散させる）

### 参照

- 設計書: `jp-meridian9/docs/INSTRUCTION-021（オープンワールド・サバイバル）.md`
- Commit: **c3bff87**

---

## 2026-05-15 — Cayo Perico ミニマップ対応＋個別死亡・restart 安全帰還の補強

### 決定・逸脱

マスターから 3 つの質問・懸念を受領し、技術検証・コード補強を実施。

**Q1: Cayo Perico の MAP（ミニマップ）対応**
- バニラ GTA V には Cayo Perico の MP DLC マップが同梱されているが、**`EnableMpDlcMaps(true)` を呼ばないとミニマップが LS のまま**だった
- `client/transition.lua` の `applyCayoPerico` / `clearCayoPerico` に `EnableMpDlcMaps(true/false)` を追加
- 追加 MAP MOD 不要

**Q2: 本島の天気・時間影響**
- 影響なし。`SetWeatherTypeNow*` / `NetworkOverrideClockTime` / `SetTimecycleModifier` / `SetArtificialLightsState` / `SetIslandEnabled` / `EnableMpDlcMaps` は **すべてクライアントローカルネイティブ**
- 任務 bucket のメンバーが個別に呼ぶので、bucket 0 の LS プレイヤーには天候・時間・色味・MAP すべて影響しない

**Q3: 死亡・タイムアウト・クラッシュの初期化処理**
- **個別死亡時の処理に重大な抜けが判明**：既存 `MRD9.Arena.OnPlayerDowned` は `downed[src]=true` のフラグだけ立て、**プレイヤーは Cayo Perico に取り残されていた**（bucket・座標そのまま）。全員ダウンで初めて `arena_wiped` が走る仕様
- 修正: `OnPlayerDowned` で **即 `Session.RemovePlayer(src, 'died')`** を呼ぶ → インベントリ消去・bucket 0・`onMissionEnd` 送信 → クライアント側 `TeleportToLosSantos` で安全帰還
- 残メンバー 0 なら既存実装 (`Session.RemovePlayer` 末尾) が自動的に `Destroy(sessionId, 'all_lost')` を呼ぶ
- `arena_wiped` の同時全滅ケースは理論上存在しなくなるが、コードは温存（互換性）
- 死亡演出 (`SetEntityHealth(ped, 1)` + `SetPedToRagdoll`) は `client/main.lua` で `reason == 'arena_wiped' or 'died' or 'all_lost'` のいずれでも発火するよう拡張
- リソース restart（クライアント側）対策: `client/transition.lua` の `onResourceStop` で **`State.active` 時に `returnPoint` へ強制テレポート**（フェードなしの保険）してから `Transition.Leave()` を呼ぶ。海面に取り残されない

### 変更ファイル

- `jp-meridian9/client/transition.lua`（`applyCayoPerico` / `clearCayoPerico` に `EnableMpDlcMaps(true/false)`、`onResourceStop` の安全帰還）
- `jp-meridian9/server/arena/arena.lua`（`OnPlayerDowned` で即 `Session.RemovePlayer(src, 'died')`）
- `jp-meridian9/client/main.lua`（死亡演出を `died` / `all_lost` でも発火）
- `jp-meridian9/docs/INSTRUCTION-021（オープンワールド・サバイバル）.md`（§13 死亡・タイムアウト・クラッシュ処理の節を追加）
- 本ファイル

### 未完了 / 次にやること

- 実機テスト:
  - ミニマップが Cayo Perico 用に切り替わるか
  - 任務中に意図的に死亡 → 自分だけ帰還＋気絶演出、他メンバー継続
  - タイムアウト・restart で異常なし

### 参照

- Commit: **49c1a89**

---

## 2026-05-15 — 帰還時の海面取り残し（ヴェガ事務所が表示されない問題）の修正

### 決定・逸脱

- 実機テスト報告: 「脱出したら本島が海しか見えず、ヴェガと看板が浮いている、見えない壁に邪魔される」（スクショ付き）。
- 原因: `TeleportToLosSantos` の処理順序が悪く、**LS の ymap がストリーミングロードされる前に Island Disable → プレイヤー移動**で、地形ジオメトリ・テクスチャ未ロード状態のままフェードインしていた。
  - 旧順序: フェードアウト → 安全化 → `Transition.Leave()` → `NewLoadSceneStart(LS)` → `SetEntityCoords(LS)`
  - 問題: プレイヤー位置が Cayo Perico 座標のまま `NewLoadSceneStart` を呼んでも、ストリーミングが LS 側を優先しない
- 修正: **先にプレイヤー座標を LS（高高度 Z+200）へ移してから** Island Disable → ストリーミング更新 → `NewLoadSceneStart` → 地面 Z 検出 → 最終テレポート
- 新順序:
  1. フェードアウト
  2. 安全化（Freeze/Invincible/Visible(false)/Collision(false)）
  3. `SetEntityCoords(LS, z+200)` で **プレイヤー位置を LS に先行移動**
  4. `Wait(200)` ストリーミング更新
  5. `Transition.Leave()` で Island/Map 破棄
  6. `Wait(800)` 破棄反映
  7. `NewLoadSceneStart(LS)` 最大 12 秒待機
  8. `GetGroundZFor_3dCoord` で地表 Z 検出
  9. 最終 `SetEntityCoords(LS, groundZ+1)`
  10. `HasCollisionLoadedAroundEntity` 最終待機 + `Wait(500)`
  11. Unfreeze + フェードイン
- `TeleportToSiteNine` 側も同じ思想で **先にプレイヤー座標を Cayo Perico 上空へ移してから IPL/Island ロード**するよう対称的に修正

### 変更ファイル

- `jp-meridian9/client/transition.lua`（`TeleportToSiteNine` / `TeleportToLosSantos` の処理順序見直し）
- 本ファイル

### 未完了 / 次にやること

- 実機: 脱出 → フェード黒幕中に LS シーンロード → フェードイン後にヴェガ事務所前で地面に立っている、海面ではない
- 任務開始側も同様に確認（北ヤンクトン版／Cayo Perico 版）

### 参照

- Commit: pending

-- 強盗開始を拒否するジョブ名（小文字推奨）

Config.BlacklistedJobs = {
  police = true,
  sheriff = true,
  leo = true,
}

-- 追加で拒否する citizenid / identifier はサーバー側で persistence と連携予定のため現状未使用

Config.BlacklistedIdentifiers = {}

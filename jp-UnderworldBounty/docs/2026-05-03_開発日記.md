# 開発日記 — 2026-05-03

## 今日やったこと（概要）

- `docs/DESIGN.md` に基づき **PHASE 0〜8** を一連で実装し、リソース一式を `jp-UnderworldBounty` としてロード可能な状態にした。
- `docs/DESIGN.md` 末尾を実装済みドキュメントへのポインタに更新した。

---

## PHASE 0: プロジェクト初期化

- `fxmanifest.lua`（cerulean / lua54 / shared・client・server・NUI）
- `README.md`（日英）、`LICENSE`（MIT）、`CHANGELOG.md`、`.gitignore`、`.cursorrules`
- ディレクトリ: `config/` `bridge/` `client/` `server/` `shared/` `locales/` `ui/` `sql/` `docs/images/` `ui/assets/`（`.gitkeep`）

## PHASE 1: Bridge と Config 基盤

- `bridge/_init.lua`（auto 検出）、`bridge/sv_bridge.lua`、`bridge/cl_bridge.lua`
- `config/*.lua` 分割、`shared/locale.lua` の `_L()`
- `locales/ja.lua` `locales/en.lua`
- サーバー命令 `/ub_test`（プレイヤーデータ・概算警官数）

## PHASE 2: シナリオ／ロケーション／サーバー検証

- `server/scenario_loader.lua`（参照整合性チェック）
- クライアントゾーン検知 + **E** で `server:requestStart`（サーバーが距離・CD・職業・警官数・アイテムを検証）

## PHASE 3: NPC と戦闘完了

- `client/npc_manager.lua`（behavior 簡易分岐、`spawn_failed` でサーバー失敗通知）
- 全滅で `server:completeCombat` → 報酬・指名手配
- `/ub_cancel` → `UbCancelHeist`

## PHASE 4: 闇の指名手配と報復

- `server/bounty.lua`（期限・ウェーブ・`wave_in_progress`）
- `client/retaliation.lua`（ローカル車両・PED、`COP` 中立オプション）
- NUI `bountyHud` と襲撃前 `client:openFlavor`

## PHASE 5: ミニゲーム

- `client/minigames.lua` + `ui/js/app.js`（lockpick / hacking / brute）
- シナリオ `entry_minigame` と連動、`server:confirmEntry`

## PHASE 6: UI/UX とロケール

- `ui/index.html` `style.css`（赤黒ベース）
- 強盗フレーバー `client:openFlavor`、HUD・トースト

## PHASE 7: イベントフックとドキュメント

- `server/events.lua` の `UbEmitHook`（`jp-UnderworldBounty:on*`）
- `docs/CONFIG_GUIDE.md` `docs/SCENARIO_GUIDE.md` `docs/EVENT_HOOKS.md`

## PHASE 8: リリース準備

- `CHANGELOG.md` v1.0.0 記載、README インストール手順整理
- **未実施（環境依存）**: 実機での triple FW 検証、Cfx.re 投稿、GitHub Release ZIP

---

## 決めたこと／メモ

- 公開フック名は `jp-UnderworldBounty:onHeistStart` 形式（AGENTS のイベント規約と整合）。
- 依存 MOD は入れない方針のため ox_lib は未使用。
- 報酬アイテムはサーバー側 `pcall` で欠損許容（未定義アイテムは無視）。

## つまずき・未決

- Qbox 単体（qb-core 無し）環境は未検証。必要なら `exports.qbx_core` 分岐を追加する。
- `TaskVehicleDriveToCoord` のパラメータは環境で調整が入る可能性あり。
- DB 永続化・報復時ドロップはスタブ／未実装。

## 次回やること

- テストサーバーで `ensure jp-UnderworldBounty` → 3 ロケーションで E 開始 → ミニゲーム → 全滅 → 報酬・HUD・報復まで通す。
- フレームワーク別に `/ub_test` と報酬アイテム名を確認。

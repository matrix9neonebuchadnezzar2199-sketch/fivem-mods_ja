# 開発日記 — 2026-05-04

前日まで: `2026-05-03_開発日記.md`

---

## PHASE 0 スタイル・プロジェクト初期化ファイルの整理

- `fxmanifest.lua` 冒頭にリポジトリ・ライセンス・設計ドキュメントへの参照コメントを追加。`client/_stub.lua` をマニフェストに追加。
- `fxmanifest.full.lua.template` を新規追加（Greenfield 向けテンプレート。現行 bridge とは構成が異なる旨を記載）。
- `.cursorrules` を拡張（ドキュメント読む順・原則・現行の `bridge/sv_bridge.lua` / `cl_bridge.lua` 前提）。
- `.gitignore` を拡張（`.cursor/`、ドラフト md、ui アセット源ファイル等）。
- `README.md` にドキュメント一覧表・現状バージョンを反映した二言語構成へ整理。
- `client/_stub.lua` と `Config.DebugUseClientStub` を追加（既定 false）。
- `CHANGELOG.md` に `[Unreleased]` を追加。
- `docs/DESIGN.md` のディレクトリツリー（`fxmanifest.full.lua.template`、`bridge`、`client/_stub`、`scenario_loader`、`locale`）を現状に合わせて修正。
- `docs/CONFIG_GUIDE.md` に `Config.DebugUseClientStub` を追記。

※ リポジトリは既に v1.0.0 ベースライン実装済みのため、マニフェストを空に戻すような「ロード不能な PHASE 0 only」状態にはしていない。

---

## Git・作業完了メモ

- 「PHASE 0 スタイル整理」を `chore(jp-UnderworldBounty): PHASE 0-style manifest refs, cursorrules, stub template` として `fivem-mods_ja` の `main` にコミット・push 済み。
- 親リポジトリの `.gitignore` で `jp-UnderworldBounty/docs/` が除外される場合がある。日記や `docs/*` をコミットするときは `git add -f jp-UnderworldBounty/docs/...` で明示追加すること。

---

## 日記ファイルの分割

- 実作業日ごとに日記を分ける方針で、本ファイルを新設。2026-05-03 ファイルから当日分の追記を移した。

---

## 追記（Git・日記分割）

- `docs(jp-UnderworldBounty): append dev diary Git memo` で日記へ Git メモを追記した後、日付の整理として `2026-05-04_開発日記.md` に移し、`2026-05-03_開発日記.md` は同日分までに限定。

---

## 追記（PHASE 1a 作業指示書）

- `docs/INSTRUCTIONS_PHASE_1A.md` を新規作成。Bridge 層を**コード変更なし**で観察し `docs/BRIDGE_API.md` にスナップショット化する手順（前提確認・grep・章立て・セルフチェック・コミット・禁止事項・完了報告フォーマット）を Cursor 自走用に一本化。
- `docs/DESIGN.md` §6 と付録、`README.md` ドキュメント表、`CHANGELOG.md` `[Unreleased]`、`.cursorrules` に相互参照または優先指示を追加。
- 所要時間の目安は指示書どおり 2〜4 時間（実際の PHASE 1a 実行時）。本追記は指示書ファイルの追加のみ。

---

## 追記（PHASE 1a 実施・BRIDGE_API スナップショット）

- `docs/BRIDGE_API.md` を新規作成（サーバー `Bridge.*` 9 関数、`ClientBridge.Notify` 1、`_init.lua` の `Framework` 検出、FW マトリクス、呼び出し元マップ、改善候補 §9）。
- `docs/DESIGN.md` §6 を「BRIDGE_API 作成済み」表現に更新、`README.md` に BRIDGE_API へのリンク、`CHANGELOG.md` `[Unreleased]` に追記。
- 実装コード（`bridge/`・`server/`・`client/`・`config/`）は**変更なし**（INSTRUCTIONS §12 準拠）。
- §2 前提: リポジトリ全体の `git status` は他プロジェクト（jp-losmon 等）で未コミット変更あり。**`jp-UnderworldBounty/` 配下のみクリーン**から着手し、コミットも当該パスのみステージ。
- 「要確認」2 箇所（Qbox 単体、`AddItem` 戻り値）。完了報告はチャットに §11 形式で別記。

---

## 追記（INSTRUCTIONS_PHASE_1A_FOLLOWUP 追加）

- `docs/INSTRUCTIONS_PHASE_1A_FOLLOWUP.md` を新規追加。PHASE 1a 完了後の「要確認」2 件の確定、`RemoveMoney`/`RemoveItem` の v1.1 再評価コメント（`sv_bridge.lua` はコメントのみ）、`BRIDGE_API.md` §9 の再分類、日記・CHANGELOG 更新を Cursor 自走用に手順化。
- `docs/INSTRUCTIONS_PHASE_1A.md` メタデータにフォローアップへのポインタを追記。`.cursorrules`・`DESIGN.md`・`README.md` に相互参照。

---

## 追記（INSTRUCTIONS_PHASE_1A_FOLLOWUP v1.1 全文差し替え）

- `docs/INSTRUCTIONS_PHASE_1A_FOLLOWUP.md` を **v1.1 で全面置換**。旧 v1.0 の会話ベース指示を破棄し、Cursor 一発完遂向けに整理。
- 主な変更点: §2 前提確認の列挙強化、`bridge/` grep パターン（`qbx-core` 等）拡張、§4.3 のコメント物理フォーマット厳守、`sv_bridge.lua` 追加行 8 行以下の明示、環境情報は日記のみ（`BRIDGE_API.md` に載せない）、§6 でフォローアップ完了後に `.cursorrules` から PHASE 1a 系優先行を削除する手順、コミットメッセージ短縮版、中断報告 §9。
- チャット投げ用: `@docs/INSTRUCTIONS_PHASE_1A_FOLLOWUP.md に従って作業してください。§2 の前提確認結果を最初に報告し、OK の場合のみ本作業へ進んでください。`
- **本追記は指示書の更新のみ**。フォローアップ実作業（BRIDGE_API 確定・コメント追加・.cursorrules 削除）は未実施なら、上記指示書に従い別セッションで実行する。

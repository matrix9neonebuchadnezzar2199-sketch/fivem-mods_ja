# 開発日記 — 2026-05-04

前日まで: `2026-05-03_開発日記.md`

---

## PHASE 0 スタイル・プロジェクト初期化ファイルの整理

- `fxmanifest.lua` 冒頭にリポジトリ・ライセンス・設計ドキュメントへの参照コメントを追加。`client/_stub.lua` をマニフェストに追加。
- `fxmanifest.full.lua.template` を新規追加（Greenfield 向けテンプレート。現行 bridge とは構成が異なる旨を記載）。
- `.cursorrules` を拡張（ドキュメント読む順・原則・現行の `bridge/sv_bridge.lua` / `cl_bridge.lua` 前提）。
- `.gitignore` を拡張（`.cursor/`、ドラフト md、ui アセット源ファイル等）。
- `README.md` にドキュメント一覧表・現状バージョンを反映した二言語構成へ整理。
- `client/_stub.lua` と `Config.DebugUseClientStub` を追加（既定 false）。
- `CHANGELOG.md` に `[Unreleased]` を追加。
- `docs/DESIGN.md` のディレクトリツリー（`fxmanifest.full.lua.template`、`bridge`、`client/_stub`、`scenario_loader`、`locale`）を現状に合わせて修正。
- `docs/CONFIG_GUIDE.md` に `Config.DebugUseClientStub` を追記。

※ リポジトリは既に v1.0.0 ベースライン実装済みのため、マニフェストを空に戻すような「ロード不能な PHASE 0 only」状態にはしていない。

---

## Git・作業完了メモ

- 「PHASE 0 スタイル整理」を `chore(jp-UnderworldBounty): PHASE 0-style manifest refs, cursorrules, stub template` として `fivem-mods_ja` の `main` にコミット・push 済み。
- 親リポジトリの `.gitignore` で `jp-UnderworldBounty/docs/` が除外される場合がある。日記や `docs/*` をコミットするときは `git add -f jp-UnderworldBounty/docs/...` で明示追加すること。

---

## 日記ファイルの分割

- 実作業日ごとに日記を分ける方針で、本ファイルを新設。2026-05-03 ファイルから当日分の追記を移した。

---

## 追記（Git・日記分割）

- `docs(jp-UnderworldBounty): append dev diary Git memo` で日記へ Git メモを追記した後、日付の整理として `2026-05-04_開発日記.md` に移し、`2026-05-03_開発日記.md` は同日分までに限定。

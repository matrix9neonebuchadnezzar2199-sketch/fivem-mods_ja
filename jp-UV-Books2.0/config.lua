Config = {}

-- ─────────────────────────────────────────────
-- 言語設定: 'ja' | 'en'
-- ─────────────────────────────────────────────
Config.Locale = 'ja'

-- ─────────────────────────────────────────────
-- フレームワーク強制指定（nil で自動検出）
-- 'esx' | 'qb' | 'qbx' | nil
-- ─────────────────────────────────────────────
Config.ForceFramework = nil

-- ─────────────────────────────────────────────
-- インベントリ強制指定（nil で自動検出）
-- 'ox' | 'qb' | 'jaksam' | 'esx_default' | nil
-- ─────────────────────────────────────────────
Config.ForceInventory = nil

-- ─────────────────────────────────────────────
-- 文字数・ページ数の上限（日本語向けに調整）
-- ─────────────────────────────────────────────
Config.MaxPages         = 20
Config.MaxCharsPerPage  = 600   -- 英語版オリジナルは 800
Config.MaxTitleChars    = 30    -- 英語版オリジナルは 20
Config.MaxAuthorChars   = 20    -- 英語版オリジナルは 15
Config.MaxGenreChars    = 30

-- ─────────────────────────────────────────────
-- アイテム名（必要なら別名に変更可能）
-- ─────────────────────────────────────────────
Config.ItemName = 'book'

-- ─────────────────────────────────────────────
-- ESX 標準インベントリ使用時の警告を表示
-- ─────────────────────────────────────────────
Config.WarnEsxDefaultInventory = true

-- ─────────────────────────────────────────────
-- デバッグログ
-- ─────────────────────────────────────────────
Config.Debug = false
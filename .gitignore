# OS
Thumbs.db
.DS_Store
desktop.ini

# エディタ・バックアップ
*.swp
*.bak
*.tmp
*.orig
# NUI i18n: whole web/dist backup dir (optional tooling)
**/web/dist.orig/
*~
.vscode/
.idea/

# GitHub 公開リポ: `.cursor` は原則除外（rules のみ共有）
.cursor/**
!.cursor/rules/
!.cursor/rules/**
# docs/ 配下は GitHub に載せない（ローカルのみ）。jp-tcgbook/docs は例外として残す
docs/
!jp-tcgbook/docs/
!jp-tcgbook/docs/**
!jp-cooktree/docs/
!jp-cooktree/docs/**
!jp-meridian9/docs/
!jp-meridian9/docs/**
!jp-lunar_fishing/docs/
!jp-lunar_fishing/docs/**

# 一時・ログ
*.log
nul

# 大きな映像（誤コミット防止）
*.mp4

# UP品: GitHub 非公開。ローカルまたは ZIP 配布のみ
jp-taxijob/
jp-lation_mining/

# Renewed-Banking 日本語派生: GitHub に含めない（ローカル・別配布のみ）
jp-renewedbanking2/

# it-drugs 系: it_bridge 等の配布制約のため GitHub から除外。ノウハウ用にローカルのみ保管
jp-it-drugs/

# ローカル素材庫（凍結された旧 RefBoard、GitHub には push しない）
RefBoard_old/

# polapaint: 凍結・GitHub 非公開（ローカルまたは別配布のみ。clone には含まれない）
polapaint/

# jp-pola（ps-camera 日本語派生）: 凍結・GitHub 非公開（ローカルまたは別配布のみ）
jp-pola/

# jp-meridian9/map: 外部 MAP リソース（The Apocalypse Project 等）の clone 先。
# 同梱しない方針（LICENSE 不明・再配布リスク回避）。GitHub には push しない。
jp-meridian9/map/

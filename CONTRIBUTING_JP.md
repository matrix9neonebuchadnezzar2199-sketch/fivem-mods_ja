# 日本語化作業の標準フロー（fivem-mods_ja）

本リポジトリで **複数 MOD を継続的に日本語化・配布する** ときの判断基準と作業の型をまとめる。  
個別 MOD の経緯は各フォルダの `README.md` / `CHANGELOG_JP.md` / `*_開発日記.md` に残す。

**注意**: `.cursor/rules` は **GitHub には載せない**（ローカル開発用）。作業の正本は本ファイルおよび各 MOD の README / 日記である。

## 前提（必読）

- プロジェクト全体方針: [AGENTS.md](AGENTS.md)
- 文字コード: **UTF-8（BOM なし）** — [AGENTS.md](AGENTS.md) の「絶対に守ること」
- 新規 **jp-\* 単体 MOD** の Lua: [AGENTS.md](AGENTS.md) のフォルダ構造・イベント命名に沿う（細目はローカルのエディタ設定で補う）

## 1. ロケール抽出の優先度（作業量の見積り）

| 状況 | 目安 |
|------|------|
| `Config.Locales` や `locales/*.lua` など **文言が一箇所に集約**されている | 翻訳・用語統一がしやすい。まずここを直す。 |
| `lib.notify` / `SendNUIMessage` / `print` などに **英語が直書き**されている | grep で拾い、可能なら設定テーブルへ寄せる（ロジックは変えない）。 |
| NUI が **ハードコード HTML/JS** のみ | 文言だけ差し替え。将来 i18n 化するなら別タスク。 |
| **jp-tcgbook** のような既存 i18n 規約がある | [AGENTS.md](AGENTS.md) の BOOK 節に従い、`ja` / `en` 両方へキー追加。 |

作業前に「集約されているか／散在しているか」を数分で確認すると、見積り精度が上がる。

## 2. 「jp-\* 単体」と「フォーク扱い」の切り分け

[AGENTS.md](AGENTS.md) の **standalone・フレームワーク非依存** は、**自前の jp-\* MOD** を指す既定ルール。

次に該当する場合は **フォーク／ローカライズ配布** として扱い、README / INSTALL で明示する。

- ESX・QBCore・ox_lib 等 **特定リソースへの依存**がある
- オリジナル作者・ライセンス・入手元 URL の記載が必要
- フォルダ構成が `jp-<名前>/オリジナルリソース名/` のように **ラッパー配下に本体**がある例（例: `jp-deliveryjobv2/nek_deliveryjobV2/`）

フォーク扱いでも、**他の jp-\* フォルダのコードを参照しない**・**BOM を付けない** 等の技術ルールは共通。

## 3. 後方互換を保つときの正規化パターン

利用者が **旧 `config.lua` を持ち込む** 想定がある場合、設定構造を拡張するときは次が安全。

- 起動時または該当機能の直前に **一度だけ** 正規化する関数を置く。
- **`nil` チェックで旧形式を検出**し、新形式 `{ ... }` に **置き換え**（または参照用にラップ）する。
- 正規化後のコードパスは **新形式のみ** を読む（分岐だらけにしない）。

参考実装: `jp-deliveryjobv2` の `NormalizeDeliveryRoutes()`（旧: `vec3` の配列のみ → 新: `{ name, stops }`）。

## 4. 翻訳・変更の対象範囲（原則）

| 対象 | 方針 |
|------|------|
| ゲームロジック・イベント名・コールバック名 | **変更しない**（オリジナル互換・チート対策のため）。 |
| ユーザー向け文言 | ロケールテーブル or NUI を **日本語デフォルト**に。 |
| 開発者・運営向けコメント | 日本語化してよい。仕様の意図が伝わるように。 |
| `fxmanifest` の `description` | 日本語化してよい（`author` はオリジナルクレジットを残す運用が無難）。 |
| CSS / アセット | **見た目を変えない限り触らない**（文言は HTML/JS/Lua 側）。 |
| `config.lua` の数値・座標 | **依頼がない限り変更しない**（運営者設定の保全）。 |

バグ修正が本体に必要になった場合は、**オリジナルへのフィードバック可否**と **配布物としての差分説明**を CHANGELOG に書く。

## 5. CHANGELOG・バージョン表記の規約（推奨）

プロジェクト内で統一できればよい。推奨は次のいずれか **一系統**。

**A. サフィックス型（いまの jp-deliveryjobv2 に近い）**

- オリジナルに追従するベース版を意識しつつ、`fxmanifest` の `version` は  
  `メジャー.マイナー.パッチ-jp`（例: `2.1.0-jp`）。
- **日本語版だけの改修**（文言、ドキュメント、後方互換正規化など）でオリジナル版号を上げない場合は **パッチ桁を増やす**（例: `2.1.1-jp`）。

**B. リビジョン型**

- `2.1.0-jp` を基準に、日本語版のみの追パッチを `2.1.0-jp.1` のように付ける。

どちらを採用するか決めたら、各 MOD の `README` か `CHANGELOG_JP.md` の先頭で一行宣言すると迷いが減る。

**Git タグ**

- リリース単位で `v2.1.1-jp` のようにタグを切り、GitHub Releases に zip を添付する運用が可能（利用者が git に不慣れでも取得しやすい）。

## 6. ドキュメントの最小セット（フォーク配布の例）

- `README.md` … 概要、依存、オリジナル URL、インストール概要
- `INSTALL.md` … 手順・よくある誤解の注釈（例: フレームワーク給与とスクリプト報酬の別建て）
- `CHANGELOG_JP.md` … 日本語版として何を変えたか
- 必要に応じて `日付_開発日記.md` … なぜその構造か、AGENTS との関係、コミット対応表

具体例: `jp-deliveryjobv2/2026-05-05_開発日記.md`

## 7. 作業後のチェックリスト（短時間）

- [ ] UTF-8 BOM が付いていない（特に Lua）
- [ ] 運営が触る `config` の座標・報酬が依頼なしで変わっていない
- [ ] 新規文言が NUI のみの場合、プレースホルダ誤用で **空座標** などが混入していない
- [ ] `CHANGELOG_JP` / `version` /（あれば）タグが整合

---

更新方針: **2〜3 MOD 分の実績で見えた共通項**が溜まったら、このファイルを追記・圧縮する。

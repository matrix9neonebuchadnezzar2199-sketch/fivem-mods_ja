# tecton-fivem 開発日記（2026-05-08 10:35）

## 概要

逆引きヘルプ原稿（**33 項目**）を `docs/ja/reverse-index.md` に統合し、`tools/build_reverse_index.mjs` で `docs/ja/reverse-index.json` と `web/src/data/reverse-index.json` を生成。ルート `package.json` の `build:reverse`、`.cursor/rules/tecton-fivem.mdc` を追加。

## 逆引きヘルプ

- 先頭: HTML コメント `<!-- SPDX-License-Identifier: LGPL-3.0-or-later -->`（Markdown 用）。
- 各項目: `---` / YAML / `---` / 見出し「状況・手順・補足」。
- ビルド: `node tools/build_reverse_index.mjs`（`npm run build:reverse`）。件数は **≥30** で合格（現状 **33**）。
- NUI: `ReverseHelp.tsx` が `web/src/data/reverse-index.json` を import（`resolveJsonModule`）。

## メモ

- 原稿の `related: group-objects` は存在しない id のため `share-scene` に差し替え。`copy-paste-coords` は `snap-to-grid` に差し替え。

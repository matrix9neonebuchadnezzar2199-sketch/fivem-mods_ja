# 開発日記 2026-05-08（M2-e Transform 仕上げ）

## 変更概要

- **クライアント**: `/tecPick` と `RegisterKeyMapping`（既定 **B**）で、プレイヤー前方 12m・カプセル半径 0.5 のレイキャストから `TectonPlacement.findSpawnedIdByHandle` で DB id を解決し、`tecton:object:get` → `selectedObject` NUI。ヒットなし／TECTON 管理外は `lib.notify`（`TECTON object ではありません`）。ビルダーが閉じていれば `ToggleBuilder()` で開く。
- **`client/placement.lua`**: `findSpawnedIdByHandle` を追加。
- **`client/nui_bridge.lua`**: `deleteObject` 成功時に `removeObject`・`spawnedHandles` 掃除・`selected` 解除・`selectedObject` を null で送信。
- **Web**: `TransformPanel` にステップチップ・±・dirty 時のみ「適用」active／「キャンセル」表示・回転正規化（送信時）・`|値| > 1e6` で `rangeError`・削除＋`ConfirmDialog`。
- **ドキュメント**: `ui-guide.md`・`spec.md`（M2-e 完了）を更新。

## ビルド

`tecton-fivem/web` で `npm run build`（2026-05-08 エージェント実行）: **成功**（`tsc -b && vite build`、約 112ms、chunk `index-BGFssnep.js` ほか生成）。

## 実機テスト（シナリオ A〜D）

以下は **FiveM サーバー上で `restart tecton-fivem` 後**に実施する想定。**本日のエージェント環境ではゲームクライアントを起動できないため結果は未実施（手元サーバーで確認すること）**。

| シナリオ | 内容 | 結果（手元で記入） |
|----------|------|-------------------|
| A | 配置 → Transform → X を +0.1×5 → 適用 → ワールド移動・サーバログ `op:update`・DB `pos_x` | 未実施 |
| B | B キーで選択 → 回転 Z=45 → キャンセルで UI 復帰・DB 不変 | 未実施 |
| C | 削除 → 確認 OK → ワールド消失・`deleted_at`・`tec_history` delete | 未実施 |
| D | パネル閉じ restart → 位置復元 | 未実施 |

## キー割当（B）について

既定の **B** はゲーム内の「ポイント」表示等と衝突し得る。運営は FiveM のキーバインド設定で `tecPick` を変更可能。`/tecPick` はキー無しでも利用可。

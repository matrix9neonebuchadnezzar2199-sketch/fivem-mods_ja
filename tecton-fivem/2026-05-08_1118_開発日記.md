# tecton-fivem 開発日記（2026-05-08 11:18）

## 概要

案B: **ShiftyWreckzz/prop-list** から `tools/import_props.mjs` で **`config/props.lua` を再生成**（ユニーク **5271** 件、重複 24 は先勝ちで警告）。`tools/category_map.json` 確定版を同梱。

## 付随

- `package.json`: `build:props`, `build:all`。
- `docs/ja/contributing-thumbnails.md`、README **Credits**、CONTRIBUTING に **GPL 由来データ**の注意。
- `build_reverse_index.mjs`: **`related` の ID 存在チェック**、**`id` 重複・必須フィールド**、空 `tags`/`steps` は WARN。

## 生成物サイズ

- `config/props.lua` 約 **1.49 MB**（環境により数バイト差）。

## 追記（同日・M1 完了〜UI スケール）

- **Milestone 1 完了**: マイグレーション（`tec_schema_version`）、サーバー DB/API、クライアント gizmo・配置、`tecton:scene:request` 復元、最小 Builder NUI（椅子3種）、実機確認済み。
- **NUI 白画面修正**: `web/vite.config.ts` に `base: './'`（`dist/index.html` が `./assets/...` を参照するよう変更）。
- **UI 1.5 倍**: `rem` 統一 + `theme.ts` の `uiScale` / `BASE_FONT_PX`、`App.tsx` で `document.documentElement.style.fontSize` 設定。規約は `docs/ja/ui-guide.md`。
- **Git**: 上記は `main` にコミット・push 済み（例: `fix(web): use relative base path…`, `feat(web): scale UI to 1.5x…`）。

## 追記（M2-a・レイアウト調整）

- **M2-a 完了（実機確認済）**: `tecton:props:fetch` で `Config.Props` を NUI へ。8 ルートの **CategoryTree**、件数バッジ、子カテゴリ展開、**react-window** の **PropList**（仮想スクロール）、配置フローまで一通り動作。
- **コミット例**: `feat(web): implement category tree and prop list for M2-a`（`a99443d` 前後）。
- **UI レイアウト調整**: ツリー文字を `theme.fontSize.bodyLarge` / `treeBadge`、パネル **左寄せ**（`padding-left: 2rem`）、**縦長**（`min-height: 85vh` / `max-height: 90vh`）、幅 **60rem**・サイドバー **20rem**、内部スクロール整理。プロップ行はやや拡大・行高 **4.5rem** 相当。
- **コミット**: `style(web): enlarge tree text, left-align panel, expand panel height`（`747d38f`）。

### 次の計画（M2 続き）

1. **M2-b**: 検索バー（モデル名・ラベル・タグ）、フィルタチップの足場。`spec.md` のインクリメンタル検索に沿って `PropList` との連携。
2. **M2-c 以降**: サムネイル（`assets/thumbnails/*.webp` と `thumb` フィールド）、カテゴリツリーの件数・表示の微調整、トランスフォーム右パネル、最近使用／お気に入り、キーバインドなど（`spec.md` §6・マイルストーン M2）。
3. **データ圧縮（任意）**: プロップ辞書が大きいため、`tecton:props:fetch` の **zlib 圧縮**は `server/api.lua` に TODO として残している。混雑サーバー・遅延が気になったら検討。

# 2026-05-03 開発日記（jp-tcgbook）

本ファイルは UTF-8（BOM なし）で保存する。

---

## §19 クローズ（M6 + 名前表示・カード i18n フォローアップ）

**判定**: M6 および後追いフォローアップを **確定** とする（実機スクショ 4 枚・観点 1〜4 確認済み）。

- **M6 後追い**: 名前表示・カード名 i18n・マーキー（2026-05-02〜03）
  - **スコープ**: ランキング Name 列の列ずれ修正、`display_name` 経路の確立、カード名 EN/JA 切替、長文マーキー横スクロール、ヘッダ右上の license → `display_name` 置換
  - **スキーマ追加**: `tcg_cards_master.name_en`、`tcg_players.display_name`（`ApplyOptionalSchemaPatches` で ALTER、duplicate 無視）
  - **コミット**: `8e11859`（Commit 1: DB 層） / `0d680ba`（Commit 2: マスタ＆identity・サーバ） / `b99d488`（Commit 3: NUI 層）
  - **ヘッダ display_name 応答**: `openBook` で `GetPlayer` が UPSERT より前のため `display_name` が NULL のまま返る件は、UPSERT 直後に `player.data.display_name = dispName` で上書きする修正で解消（コミットメッセージ `fix(jp-tcgbook): openBook merge display_name after UpsertPlayerDisplayName`）
  - **確認済み（実機スクショ）**:
    - ランキング Name 列の正しい配置（Rank / Tier / Name / Rating / Lv / EXP / Streak の 7 列）
    - 自分行サマリの 3 段構造（順位 → 名前 → Tier → ステータス）
    - カード名 EN/JA 切替（コレクション・詳細パネル・対戦アリーナ）
    - `description_en` の連動
    - `tcg_match_history.display_name_a/b` の埋め込み（Commit 2）
    - `shared/cards.lua` 全 20 件 `name_en`、`shared/identity.lua` の `GetPlayerDisplayName`（QBox → QBCore → ESX → `GetPlayerName` → `player:<src>`）
  - **運用メモ**: 既存 DB の `display_name` は次回 `openBook` / 実 PvP Finish で UPSERT され自然に埋まる

- **ロードマップ**: M1〜M6 完全完了。M7 PHASE E6（シーズン制）は任意・後送り。次の本線は CFX フォーラム告知準備、または PHASE B（実 PvP）。

- **CFX 告知前クリーンアップ（設定・メタ・grep 棚卸し）**
  - **本番デフォルト**: `Config.Debug` / `DebugCommands` / `BattleWireLog` は **`false`**（`SeedCardsFromLua = false`・ランキング系・`RankingExcludeCitizenids` は据え置き）
  - **`[tcg-trace]`**: 実コードから削除済み。`git grep tcg-trace` のヒットは日記・`docs/verify` の履歴説明のみ
  - **`[jp-tcgbook][wire]`**: サーバ／クライアントは **`TcgBattleWireLogEnabled()`** 等でガード（ランキング送信ログ含む）。Wire を見るときだけ `BattleWireLog = true` または `Debug = true` に戻す

---

## §20 キャラカード画像の `tcg_<card_id>.jpg` 統一（モンスター枠と同型）

**目的**: `html/assets/cards/character/` のランダム名 JPG を、マスタの `card_id` と一致するファイル名にそろえ、`shared/cards.lua` の `image_path` と運用上の突き合わせを簡単にする。

### 実施内容

1. **リネーム（20 枚）**  
   モンスター側と同様、`tcg_<card_id>.jpg` に統一（例: `tcg_ur_antares.jpg`、`tcg_c_slime.jpg`）。

2. **`shared/cards.lua`**  
   先頭 20 枚（キャラ）の `image_path` をすべて  
   `assets/cards/character/tcg_<card_id>.jpg` に変更。ファイル先頭コメントも「キャラは character の JPG・card_id ベース」と明記。

3. **レガシー `tcg_ch_001.png`〜`tcg_ch_100.png`**  
   リポジトリ上は削除（プレースホルダ PNG の列から、実カード 20 枚の JPG へ置き換え）。

4. **`html/assets/cards/asset_manifest.json`**  
   旧 `tcg_ch_*.png` および旧モンスター PNG 列が残っていたため、`cards.lua` の並びどおり **キャラ 20 + モンスター 21（計 41）** の実在 JPG パスだけに整理。

### Git（push 済み）

| コミット | 内容 |
|---------|------|
| `51517d7` | キャラ JPG リネーム、`cards.lua` 同期、`tcg_ch_*.png` 削除 |
| `e822b90` | `asset_manifest.json` を実ファイル・マスタ並びに同期 |

### 運用メモ

- マスタ変更を DB に反映するときは **`Config.SeedCardsFromLua = true`** でシード → 検証後 **`false`** に戻す。
- カードの **日本語名・説明文** は従来マスタのまま。イラストと文言を完全一致させる場合は、`name` / `name_en` / `description` / `description_en` の別タスクで更新する。

### 任意（絵と名前の対応が気になる場合）

割当は職種・雰囲気ベースのため、ゲーム内で見てズレがあれば同名ファイルの **差し替え**または **対応表の入れ替え**で調整可能。

### 追記（マスタ文案・実機確認用）

- **`tcg_m_a_demon_warlord`**: イラストは大型斧系のため、`description` / `description_en` を双刃槍から **両手大斧** に合わせて修正（`5bf01df`）。シード時に DB へ反映。

---

## §21 NUI: コレクションタイルの固定サイズ・デッキ編成ボタン拡大

### 背景

- コレクションのサムネが **元画像の縦横比・内在的サイズ** の影響でグリッド上のカード枠の見た目がばらついていた。
- デッキ編成の **スロット削除（×）** と **所持カードの追加（+）** が小さく、視認性・押しやすさに課題があった。

### コレクション／デッキ（所持カードペイン）のタイル

**ファイル**: `html/css/collection.css` / `html/css/deck.css`

- グリッドに **`align-items: start`**（行の最大高に引き伸ばさない）。
- カード枠: **`width: 100%`**, **`min-width: 0`**, **`overflow: hidden`**。
- イラスト枠（`.card-art` / `.slot-art`）: **`flex: 1 1 0`**, **`min-height: 0`**, **`overflow: hidden`**（フレックス子の `min-height: auto` による伸長を抑止）。
- 画像: **`object-fit: cover`**, **`object-position: center`**, **`display: block`** 等で **固定枠内に収める**。
- コレクションの **`.card-name`**: デッキ同様 **2 行クランプ**（長い名前で枠高が不揃いになるのを防止）。

**コミット**: `0104b8a`（push 済み）

### デッキ編成: 削除ボタン・追加ボタン

**ファイル**: `html/css/deck.css`

- **`.slot-remove`**: `calc(12px * var(--book-fs-mul))` ＋ `min` 28px、× 文字拡大、枠 2px・影で視認性向上。`:active` で軽い縮小フィードバック。
- **`.add-btn`**: `calc(17px * var(--book-fs-mul))` ＋ `min` 44px、非ホバー時 `scale(0.72)`、**+** を `calc(24px * var(--book-fs-mul))`、ボーダー・グラデ強化。`:active` フィードバック。

**コミット**: `dcac05e`（push 済み）

---

## 参照

- 同日以前の検証・PHASE 記録: `2026-05-02 開発日記.md`、`2026-05-01 開発日記.md` §19

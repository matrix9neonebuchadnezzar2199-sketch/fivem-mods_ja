# 開発日記 — 2026年5月1日（金）

**jp-tcgbook 設計図（累積）兼 開発日記** — 実装の優先順位・仕様の正は **本ファイル** とする。

**保管場所**: このファイルは **`jp-tcgbook/` 配下のみ**に置く（親リポジトリの他リソース用ログと混同しないため）。

---

## 設計図の正と PHASE 運用

### このファイルの役割

- **jp-tcgbook の設計図・意思決定・フェーズ計画の単一の正（single source of truth）** とする。実装・コードレビュー・エージェントへの指示は、**ここに書いた内容と矛盾しないこと**を優先する。
- `README.md`・`docs/*.md` は要約・手順の補助。**本文と食い違う場合は、まず本ファイルを更新**し、続けて補助ドキュメントを追随させる。

### PHASE の進め方（実行前に必ず記録）

次の順序を守る。**コードを書き始める前**に、対象 PHASE の記述を本ファイルへ追記する。

1. **記録**: 下記テンプレに沿って **目的・非スコープ・受け入れ条件・変更予定（サーバー／クライアント／NUI／DB）・タスク・リスク** を **このファイルに追記**する。
2. **すり合わせ**: 曖昧な要件・優先度はこの段階で潰す（一人開発でも、未来の自分向けに検証可能な形にする）。
3. **実行**: 実装・テスト・デプロイ。完了した項目は同一 PHASE 節内で **チェック／実装済みメモ** を残す。

### PHASE 記録テンプレート（新規 PHASE 追記時にコピー）

```markdown
### PHASE N — （短いタイトル）

| 項目 | 内容 |
|------|------|
| 状態 | 計画中 / 実装中 / 完了 |
| 開始日 | YYYY-MM-DD |
| 完了日 | （完了時に記入） |

#### 目的・背景

（なぜこの PHASE か）

#### スコープ（やること）

- …

#### 非スコープ（明示的にやらないこと）

- …

#### 受け入れ条件（検証可能）

- [ ] …

#### 設計・変更予定

- サーバー: …
- クライアント: …
- NUI: …
- DB（マイグレーション）: …

#### タスク一覧

- [ ] …

#### メモ・依存・リスク

- …
```

**PHASE 1（現状）**: 本章「詳細設計図」および §13 対応表に実装内容が集約されている。**仕様変更やフェーズ2以降**は、上記テンプレで **新規節をこのファイルに追加**してから手を動かす。

### PHASE 2 — 対戦タブ（足がかり → 盤面）

| 項目 | 内容 |
|------|------|
| 状態 | 実装中 |
| 開始日 | 2026-05-01 |

#### 目的・背景

ロードマップ A（対戦・バトル盤面）に向け、NUI で **使用デッキの可否を即座に把握**できるようにする。`openBook` 時点で **アクティブデッキ詳細**を渡し、以降のマッチング・3×3 盤・手札5への拡張の土台とする。

#### スコープ（やること）

- [x] `openBook` 応答に **`activeDeck`**（★デッキの `GetDeck` 相当）を含める
- [x] NUI **対戦タブ**: 使用デッキ名・10枚充足・レート・（準備OK時）総合PWR、デッキ編成への導線
- [x] `deckSelected` / `deckUpdated` で **編集中デッキが使用デッキと同一のとき** `AppState.activeDeck` を同期
- [x] 準備パネル: **`activeDeck` 未取得時はスケルトン＋再読込**（`selectDeck`）
- [x] **仮想対戦ロビー**（マッチングなし）: 待受側が **サーバーID** を伝え、呼び出し側が番号入力で接続（`server/battle_lobby.lua`）。BOOK 閉じる／`battleVirtualLeave` でセッション解除
- [x] `openBook` に **`ui.playerServerId`**・再接続用 **`battleSession`**
- [x] 仮想対戦中の **BOOK 閉じる／切断**に **確認ダイアログ**（誤操作防止）

#### 非スコープ（この節ではやらないこと）

- 自動マッチング・近距離招待、サーバー権威の対戦ループ、3×3 盤、手札・ドロー、**敗北時コピー移転のサーバー実装**、**パック購入（排出レンジ・設定 UI 含む）**、Elo 本番更新

#### カード入手経路（プロダクト方針・2026-05-01）

- **トレード機能は廃止**（プレイヤー間の任意交換 UI／サーバー処理は行わない）
- **対戦**: **敗北したプレイヤーは、相手に自分のデッキからカードを 1 枚ランダムに選び「コピー」として渡す**（元デッキの枚数は減らない。実装・監査ログは別 PHASE）
- **●パック（10枚）購入**: 入手経路として採用するが、**排出レンジ・運用設定 UI 等は別途仕様書で示すまで保留**（実装も保留）

#### 対戦終了・異常終了・ヒューマンエラー対策（方針・2026-05-01）

- **意図しない終了**: 仮想対戦 **切断**・BOOK を **ESC で閉じる**とき、接続中なら **`window.confirm` による確認**を挟む（現状実装）。将来的に盤面タブ内の「投了／終了」も同様に **確認モーダル or 二段階**を検討する
- **ゲーム／クライアント落ち**: 相手には `playerDropped` 等で **終了通知**（現状）。再接続・途中再開・不戦敗の扱いは **別 PHASE で設計**（サーバー権威の状態機械が必要）
- **誤タップ**: 上記に加え、破壊的操作は **文言を明示**（「相手側も切断」等）し、必要なら **モーダル＋チェックボックス**へ格上げする

#### 受け入れ条件

- [x] BOOK 初回オープン直後、対戦タブで **使用デッキの枚数状態**がブレずに表示される
- [x] 使用デッキを編集して保存後、対戦タブの表示が追随する（同一セッション内）

---

### PHASE A 草案 — 本番対戦ルール整理・「対戦」UI テキストモック（2026-05-01 追記）

| 項目 | 内容 |
|------|------|
| 状態 | **草案**（実装前のすり合わせ用。確定したら受け入れ条件をチェックし `状態: 確定` へ） |
| 目的 | ロードマップ **A** の **ルール**と **NUI「対戦」タブ**の拡張イメージを一文書化する |

#### A-1. 既に確定していること（設定・既存方針）

| 項目 | 内容 |
|------|------|
| デッキ枚数 | **10枚**（`Config.DeckSize`） |
| 手札枚数（対戦時） | **5枚**（`Config.HandSize`、フェーズ2向けに既に定義） |
| 盤面 | **3×3**（9マス）。§15 技術メモと整合 |
| シャッフル | **サーバー権威**（クライアントの任意シャッフルは信頼しない） |
| カード能力 | 各カード **上下左右 1〜10**（マスタ通り） |
| 入手（勝敗後） | **敗北側のデッキから 1 枚ランダムを「コピー」で勝者へ**（所持・デッキ枚数は減らない）。**実装は別 PHASE** |
| トレード | **なし** |
| 現状の「対戦」タブ | **使用デッキ準備**＋**仮想ロビー（サーバーID）**のみ。盤面・手札は未実装（`battle.js` フッタ注記と同じ） |

#### A-2. ルール草案 v0.1（未確定 — 実装前に Yes/No を取る）

**コア仮定**: 双方向の数値比較で隣接マスを「奪取」する方式（一般に「盤上に枚を置き、辺同士を比較する」系。名称は未固定）。**別案**（下記 A-4）に切り替え可能。

1. **試合前**
   - 各プレイヤーは **使用デッキ 10 枚**を用いる。
   - サーバーが各デッキを **独立にシャッフル**し、各プレイヤーに **先頭 5 枚を手札**、残り **5 枚を山札**（ドロー用）として配る。
   - **先攻／後攻**はサーバー乱数（または将来ルールで固定）。

2. **ターン構造**
   - 先攻から **交互に 1 ターン**。
   - 自分のターンにできること（草案）: **空マスを 1 つ選び、手札から 1 枚を表向きで配置する**。その後、山札が残っていれば **1 枚ドロー**（手札上限 **5** のときは **ドローしない**／または上限を超えない範囲でドロー、**要決定**）。
   - **パス**の有無: 現草案では **不要**（9 マス埋まるまで交互に配置）。
   - **数え上げ**: 盤が交互占有になると **先攻 5 手・後攻 4 手**になる。**ドロー規則**を変えると山札消費が変わるため、ルール確定後に **9 手分の表**（各時点の手札枚数・山札残り）で破綻がないか検証する。

3. **配置直後の判定（奪取ルール案）**
   - 今置いたカードについて、**上下左右に隣接するマス**に **相手所有**のカードがある場合、その **共通辺**について数値を比較する。
   - **例**: 今置いたカードの **右辺** と 右隣の相手カードの **左辺**。置いた側の数値 **＞** 相手の数値なら、**右隣のマスを自分側に奪取**（所有色／オーナーを切り替え）。
   - **同数**のとき: **奪取しない**（草案）。または **先に置いていた側優位** 等は **要決定**。
   - **同一ターンに複数隣接**がある場合、**そのターンに成立した隣接すべて**で順に判定（**連鎖奪取**は **Phase A ではオフ**を推奨：実装簡素化）。

4. **終了と勝敗**
   - **9 マスがすべて埋まったら終了**。
   - **勝者**: 終了時点で **自分の所有マス数が多い**プレイヤー。**同数**は **引き分け**（草案。レート変動の扱いは F PHASE で決定）。

5. **サーバー権威**
   - 盤面・手札・山札・乱数・ターンステップはすべて **サーバーが正**。クライアントは **要求**（マス座標＋手札インデックス等）のみ送り、**結果**をイベントで受け取る。

#### A-3. 「対戦」タブ UI — テキストシミュレーション（現状 + 拡張イメージ）

**■ 画面 A — 現状（ロビー前・実装済みに近い）**

```
┌─ BOOK > [対戦] ─────────────────────────────────────────────┐
│ 【使用デッキ】                                                │
│   マイデッキ                                                  │
│   状態: 対戦準備OK（10枚）                                    │
│   レート: 1500                                                │
│   総合PWR: 42                                                 │
│   [ デッキを確認・編集 ]                                      │
├──────────────────────────────────────────────────────────────│
│ 【仮想対戦（デバッグ／検証）】                                │
│   あなたの番号（サーバーID）: 1                               │
│   [ 招待待機を開始 ]                                          │
│   相手の番号: [____]  [ 呼び出す ]                            │
│   ※ 盤面・手札・報酬は未実装（ロビー接続のみ）               │
├──────────────────────────────────────────────────────────────│
│ （フッタ）マッチング・3×3 盤・手札5・サーバー権威ルールは     │
│          今後のフェーズで実装予定です。                        │
└──────────────────────────────────────────────────────────────┘
```

**■ 画面 B — 拡張イメージ（マッチ成立〜対戦中・未実装）**

ロビー接続後、同一タブ内で **パネル差し替え**または **折りたたみ**を想定。

```
┌─ BOOK > [対戦] — 対戦中 ─────────────────────────────────────┐
│ 相手: サーバーID 12    先攻: あなた    ターン: あなた        │
├──────────────────────────────────────────────────────────────│
│                     【フィールド 3×3】                        │
│            [1]    [2]    [3]                                 │
│         [4]    [5]    [6]                                    │
│              [7]    [8]    [9]                               │
│   ※ 空マスは [ ]、配置済みは [カード略称] またはサムネ       │
│   ※ マスをクリック → 手札から選んだカードを置く（サーバー確定）│
├──────────────────────────────────────────────────────────────│
│ 【あなたの手札】（最大5）                                      │
│   [ カ1 ] [ カ2 ] [ カ3 ] [ カ4 ] [ カ5 ]  ← 選択ハイライト   │
├──────────────────────────────────────────────────────────────│
│ 【ログ】                                                       │
│   ・ターン1 あなた: (5,2) に「苔森のスプライト」配置           │
│   ・奪取なし                                                   │
│   ・ターン2 相手: (4,2) に「…」配置 …                         │
├──────────────────────────────────────────────────────────────│
│ [ 投了（確認） ]     山札残: 3（自分のみ表示するか要決定）     │
└──────────────────────────────────────────────────────────────┘
```

**UI メモ（修正候補）**

- **手札と場の操作順**: 「先にマス選択 → 手札選択」か「先に手札 → マス」か。**モバイル寄りなら手札先**が誤タップ少なめのことが多い。
- **相手手札**: 通常は **裏**または **枚数のみ**表示（情報開示はルール次第）。
- **奪取演出**: ログ＋マス枠色変化で最低限。アニメは後追い可。
- **投了**: 確認ダイアログ必須（既存方針と整合）。

#### A-4. ルールの代替案（比較検討用）

| 案 | 概要 | メリット | デメリット |
|----|------|----------|------------|
| **隣接数値比較（A-2）** | 置いた枚の辺で隣の相手と比較し奪取 | 四方向ステをそのまま活かせる | Triple Triad 系に寄る／連鎖ルールの議論が出やすい |
| **配置のみ・奪取なし** | 9 マスに交互配置し、終了時に **合計PWR** や **最大辺の和** で勝敗 | 実装が軽い | 「対戦感」は弱め、カード差が偏りやすい |
| **1on1 セル対決** | 各マスで「そのマスに置いたカード同士」だけ即決（隣接無視） | ルール説明が短い | 3×3 の意味が薄れる |

#### A-5. 確定前チェックリスト（すり合わせ用）

- [ ] 終了条件は「9 マス埋まり」でよいか（**時間制限**は不要か）。
- [ ] ドローは「配置後に常に 1 枚」か、**手札5上限**で **引けないターン**を許容するか。
- [ ] **同数**の辺は **不奪取**でよいか。
- [ ] **連鎖奪取**（奪取で新たな隣接が生まれ再判定）は **当面オフ**でよいか。
- [ ] **敗北時コピー**は「試合終了直後にサーバーが選ぶ」でよいか（カードプールは **使用デッキ 10 枚のマスタID**から）。

---

### 今後の PHASE ロードマップ（大項目・2026-05-01 追記）

以下は **マイルストーン／バックログ**。**実装順はここでは固定しない**。着手するときは、対象ブロックを **テンプレの `PHASE N` として本ファイルに詳細化**してから実行する。

| # | 領域 | メモ |
|---|------|------|
| A | **対戦タブ・バトル盤面** | 3×3 盤・手札・ドロー・サーバー権威のルールなどは §15 技術メモも参照 |
| B | **マッチング機能** | 自動キュー等は別 PHASE。**当面はサーバーID＋招待待機の仮想対戦**で検証 |
| C | **~~トレード~~** | **廃止**。入手は **対戦（敗北側デッキから1枚ランダム・コピー付与）** と **パック（10枚・仕様保留）** のみ |
| D | **ランキングタブ** | **仕様検討中**（プレースホルダのみ）。表示・並び・更新はレート連動で後決め |
| E | **カード画像アセット** | **プレースホルダは絵文字で可**（本番 PNG は `html/assets/cards/…` 方針は「本日の記録」参照）。欠損時フォールバックを UI 共通で決める |
| F | **レーティング更新ロジック** | `Config.EloKFactor` 等の **本番反映**、対戦終了時の更新フロー |
| G | **管理者：カードマスタ CRUD 画面** | 起動 **`/bookadmin`**。**一般 UI と別ページ**。レイアウトは §1「管理者 UI レイアウト仕様」。画像・四方向数値・ランク・指定／フリー・説明。**削除**含む |

#### G に関する設計上の必須条件（エンドユーザー BOOK との整合）

マスタの **追加・編集・削除** は管理者 UI から行う想定だが、**一般プレイヤーの BOOK が常に破綻なく追随できる**ように設計すること（実装 PHASE を分けてもよいが、要件として固定）。

- **権威**: マスタは **DB（＋必要ならストレージ上の画像）を正**とし、BOOK はサーバー経由のデータのみ表示・編集対象とする（ローカル固定マスタのみに依存しない）。
- **追加・編集**: 開いているクライアントへ **再同期または差分通知**の方針を PHASE 記録で決める（リロード・イベント・次回 open だけでよいか等）。
- **削除**: 事前にポリシーを決める（例: 所持 `tcg_player_cards` の扱い、デッキ `tcg_deck_cards` の該当スロット、対戦中の参照）。**プレイヤーから見て説明可能な挙動**にすること。
- **画像**: パス欠損・差し替え時も **絵文字等のフォールバック**で BOOK が落ちないこと。

---

## 本日の記録：カード画像アセットの管理方針（決定）

**結論（実装・運用の正とする）**

- **ルート**: `jp-tcgbook/html/assets/cards/`（NUI の `ui_page` が `html/` なので、`image_path` は `assets/cards/...` のように **html からの相対**で統一するとよい）
- **第1階層（コンテンツ種別）**: 次の3フォルダに分類する  
  - `monster/` … モンスター  
  - `item/` … アイテム  
  - `character/` … 人物  
- **指定（shitei）／フリー（free）**: **ディレクトリでは分けない**（ルールは DB / `shared/cards.lua` の `type` で持つ）
- **レア度（UR〜C）**: **基本はパスに含めない**（バランス変更でファイル移動が増えるため）。枚数が非常に多くなった場合のみ、任意でサブフォルダを検討
- **ファイル名**: マスタと1対1になるよう **`{card_id}.png`** を推奨（例: `assets/cards/monster/tcg_s_flame_fist.png`）

**fxmanifest**: PNG を配信する際は `files { ... 'html/assets/cards/**/*.png' }` 等で明示（現状コメントのみの場合は追加タイミングで対応）。

---

## jp-tcgbook — 詳細設計図（これまでの実装を含む累積）

本節は **FiveM リソース `jp-tcgbook`** のアーキテクチャ・データ・UI・運用を一文書にまとめたもの（上記「設計図の正」に従う技術的な累積）。改訂版フェーズ1指示書・これまでの実装チャット・本日の画像方針を反映する。**将来の PHASE は、冒頭テンプレで先に追記してから** §13 の対応表や本章を更新する。

### 1. プロジェクト概要

| 項目 | 内容 |
|------|------|
| 名称 | jp-tcgbook（BOOK） |
| 位置 | 親リポジトリ `fivem-mods_ja` 内の `jp-tcgbook/`（単体 `git init` しない） |
| コンセプト | ハンターハンターの「グリードアイランド」× FF8「トリプルトライアド」系の **PvP カードバトル**を目標としたリソース |
| **フェーズ1（現状スコープ）** | **`/book`** で開く一般向け UI／**コレクション**／**デッキ編成**／初回配布／DB／**デバッグコマンド** |
| **フェーズ2以降** | 冒頭 **「今後の PHASE ロードマップ」** の A〜G（対戦・マッチング・~~トレード~~・ランキング・画像・レート・管理者 CRUD）。**トレードは廃止** |

#### BOOK 起動コマンドの分担（決定・追記）

| コマンド | 用途 |
|----------|------|
| **`/book`** | **一般プレイヤー**向け BOOK。コレクション・デッキ編成・対戦／ランキング（検討中）等。**トレードタブは無し**。 |
| **`/bookadmin`** | **運営（管理者）**向け画面の起動。カードマスタ CRUD・画像設定など（ロードマップ **G**）。 |

一般 **`/book`** の NUI と **`/bookadmin`** は **完全に別 UI（別 HTML／別エントリ）** とする。同一 `index.html` でのモード分岐は行わない。

`/bookadmin` の **権限チェック（ACE 等）はサーバー／クライアント側で必須**（実装時）。`ui_page` を管理者用に差し替える／別ファイルを `SendNUIMessage` で読む等の **実装手段は PHASE G で決定**。

#### 管理者 UI（`/bookadmin`）レイアウト仕様（追記）

**カラム比率**: 左 **20%** ／ メイン **50%** ／ 右 **30%**（合計 100%）。

| 領域 | 幅 | 内容 |
|------|-----|------|
| **① 左サイドバー** | 20% | 登録済みカードを **フォルダツリー**表示（第一階層: **キャラクター**／**モンスター**／**アイテム**）。**検索**、**ランク**・**指定／フリー**等の**フィルター**。**新規追加**でメインが **空の default 状態**になる。 |
| **② メイン** | 50% | ①で選択したカードの編集。**中央に画像**、**上下左右に数値**の入力／選択、**レアリティ**・**指定／フリー**等の各種選択、**説明文**入力。 |
| **③ 右ペイン** | 30% | **比較用カード一覧**。メインで数値を決めるとき他カードとの比較に使う。**検索・フィルタは標準搭載**。 |

**テキスト配置図（ワイヤー・細部は実装・別指示で調整）**:

```text
┌─────────────────────────────────────────────────────────────────────────────────┐
│  /bookadmin（管理者・別 NUI）                                                     │
├──────────────┬──────────────────────────────────────┬───────────────────────────┤
│ ① 左 20%     │ ② メイン 50%                         │ ③ 右 30%                  │
│              │                                      │                           │
│ [検索____]   │   ┌────────────────────────────┐    │ [検索____] [フィルタ…]    │
│ [フィルタ]   │   │      [画像プレビュー]       │    │                           │
│              │   │         （中央）            │    │  ┌───┐ ┌───┐ ┌───┐ …    │
│ ▼キャラクター │   └────────────────────────────┘    │  │mini│ │mini│ │mini│     │
│   ├ card…    │              [上: 数値]              │  └───┘ └───┘ └───┘       │
│   └ …        │   [左]   [右]                      │  （比較用・スクロール可）  │
│ ▼モンスター   │              [下: 数値]              │                           │
│   └ …        │   レア / 指定・フリー / その他       │                           │
│ ▼アイテム     │   [説明文____________________]       │                           │
│              │                                      │                           │
│ [＋ 新規追加] │  ※新規時: 上記が空の default         │                           │
│ → メイン初期化│                                      │                           │
└──────────────┴──────────────────────────────────────┴───────────────────────────┘
```

**設計メモ（任意・後続 PHASE で検討）**

- **未保存警告**（離脱・閉じる・別カード選択時）
- **card_id** の命名規則・重複チェック・DB 既存行との整合
- **削除**時は確認＋所持／デッキ影響の説明（ロードマップ G の必須条件と連動）
- **右ペイン**に「ピン留め」比較（2〜3 枚固定表示）があると数値調整が楽
- **画像**はフォルダ列挙リストから選ぶ／パス直打ち＋プレビュー（サーバー API で一覧化）
- 変更**監査ログ**（運営 ID・時刻）は本番運用で検討

### 2. 技術スタック

- FiveM: `fx_version 'cerulean'`, `game 'gta5'`, `lua54 'yes'`
- サーバー／クライアント: Lua
- NUI: 素の HTML / CSS / JS（ビルドなし）
- DB: MariaDB / MySQL（**oxmysql** 必須）

### 3. フレームワーク・識別子方針

- **ESX / QBCore 非依存**（スタンドアロン）
- DB 列名は歴史的に **`citizenid`**。実体は **`shared/identity.lua` の `GetPlayerUid(source)`** で決定  
  - 優先順位: **license → discord → fivem → steam**（いずれも無ければ `nil`）
- 将来フレームワーク連携する場合も **この関数だけ差し替え**可能な設計とする

### 4. Git 運用

```text
cd <親リポジトリルート>
git add jp-tcgbook/
git commit -m "feat: ..."
git push
```

### 5. ディレクトリ構成（実装済みの要点）

```text
jp-tcgbook/
├── fxmanifest.lua
├── config.lua
├── README.md
├── .gitignore
├── 2026-05-01 開発日記.md      # ★本リソースの開発ログ・設計累積（このファイル）
├── shared/
│   ├── identity.lua
│   └── cards.lua              # TcgCardsMaster（起動時 UPSERT）
├── server/
│   ├── database.lua
│   ├── collection.lua
│   ├── deck.lua
│   ├── debug.lua              # /tcg_* デバッグコマンド
│   ├── main.lua               # NetEvent 集約
│   └── sql/install.sql
├── client/
│   ├── main.lua               # /book, NUI メッセージ受信, forceClose
│   └── nui_callbacks.lua
├── html/
│   ├── index.html
│   ├── css/common.css, collection.css, deck.css
│   └── js/nui.js, api.js, card_util.js, collection.js, deck.js, app.js, mock_data.js
├── docs/
│   └── IMPLEMENTATION_PHASE1.md   # フェーズ1メモ・デバッグ一覧（短版）
├── デザイン仕様/                  # UIモックの正（collection.html, deck.html 等）
└── html/assets/cards/             # ★画像配置先（monster/item/character を想定）
```

- **`デザイン仕様/`** をモックの正とし、`docs/mocks/` への複製は行わない（README にも記載済み）

### 6. データベース設計（概要）

| テーブル | 役割 |
|----------|------|
| `tcg_players` | プレイヤー1行（initialized, rating, wins, losses, draws） |
| `tcg_cards_master` | カードマスタ（card_id, rank, type, 四方向ステ 等） |
| `tcg_player_cards` | 所持インスタンス（citizenid + card_id、FK は card_id → master） |
| `tcg_decks` | デッキ定義（citizenid, name, is_active、**同一プレイヤー内デッキ名 UNIQUE**） |
| `tcg_deck_cards` | デッキスロット1〜10（deck_id, slot_index, card_id、deck 削除時 CASCADE） |

**リセット順（明示削除の保険）**: `tcg_deck_cards` 関連 → `tcg_decks` → `tcg_player_cards` → `tcg_players`（`Database.ResetPlayer`）

**マスタ同期**: `shared/cards.lua` を起動時 **UPSERT**（削除されたマスタの DB 自動削除はしない方針）

### 7. ゲームルール（デッキ／カード）要点

- 1デッキ **10枚**、1プレイヤー最大 **10デッキ**
- **指定（UR/SS）**: デッキ内 **合計2枚まで**、同名 **1枚まで**
- **フリー（S〜C）**: フリー枠 **8枚**、同名 **2枚まで**
- **使用デッキ（アクティブ）**: **10枚揃ったデッキのみ**設定可
- カードは四方向 **1〜10**。対戦時の**盤上ルール（奪取・勝敗）**は **「PHASE A 草案」**で整理中。確定後、必要なら本節に1行要約を残す

### 8. サーバー設計

**読込順（`fxmanifest.lua`）**

1. `@oxmysql/lib/MySQL.lua`
2. `database.lua` → `collection.lua` → `deck.lua` → **`debug.lua`** → `main.lua`

**担当**

| モジュール | 責務 |
|------------|------|
| `database.lua` | CRUD、テーブル初期化、UPSERT、Reset / ClearCards / Rating / ListPlayers 等 |
| `collection.lua` | 初回配布、所持取得、Initialize |
| `deck.lua` | デッキ CRUD、編成バリデーション、CanAdd / Add / Remove、Duplicate、Active |
| `debug.lua` | ACE 付き `/tcg_*` |
| `main.lua` | `jp-tcgbook:server:*` の入口、`buildDeckListPayload`、`openBook` 等 |

**イベント命名**: `jp-tcgbook:server:<action>` / `jp-tcgbook:client:<action>`（詳細は `main.lua`・`client/main.lua`）

**権威**: 所持・デッキ・バリデーションは **サーバー決定**。NUI は表示と入力のみ。

### 9. クライアント・NUI

- **`/book`**（一般）: `SetNuiFocus(true,true)` → `open` → `TriggerServerEvent('jp-tcgbook:server:openBook')`
- **`/bookadmin`**（管理者）: **一般 BOOK とは別 HTML** の NUI を開く。`RegisterCommand('bookadmin', …)` → `SetNuiFocus` → 管理者用 `open`／サーバー検証（ACE）。詳細は PHASE G。現コードは **未実装の場合あり**。
- 結果は **方式B**: サーバー応答を `TriggerClientEvent` → `SendNUIMessage({ action, payload })`
- ESC: ヘルプ優先 → BOOK 閉じる（`closeBook`）
- **リセット時**: `jp-tcgbook:client:debugForceCloseBook` でフォーカス解除 + `forceClose` メッセージ

**タブ構成（index.html）**

- コレクション／デッキ編成: **実装済み（3ペイン等）**
- 対戦: **準備パネル＋サーバーID 仮想ロビー**。ランキング: **検討中プレースホルダ**。**トレードタブなし**

**主要 JS**

| ファイル | 役割 |
|----------|------|
| `nui.js` | message ディスパッチ、`fetch` / mock 振り分け |
| `api.js` | `NUI.send` ラッパ |
| `card_util.js` | 絵文字・マスタ正規化・ミニステ最大値ハイライト・cardPower |
| `collection.js` | `window.Collection` |
| `deck.js` | `window.Deck` |
| `app.js` | `AppState`、タブ、モーダル、NUI リスナー |
| `mock_data.js` | ブラウザ単体用（マスタ120・所持47 等、本番 Lua マスタとは別データ） |
| `battle.js` | 対戦タブ（準備パネル・仮想ロビー UI） |
| `server/battle_lobby.lua` | 招待待機／番号呼び出し／仮想セッション |

### 10. config.lua（要点）

- `Config.MaxDecksPerPlayer`, `Config.DeckSize`, `Config.HandSize`, `Config.MaxShiteiPerDeck`
- `Config.CardLimit.shitei` / `free`
- `Config.InitialCards`, `Config.InitialCardRanks`
- `Config.InitialRating`, `Config.EloKFactor`
- `Config.Debug`, `Config.DebugCommands`
- `Config.AutoSaveDebounceMs`（NUI デッキ **＋／×** のデバウンス送信に使用）

### 11. デバッグコマンド（実装済み一覧）

**権限**: **`Config.DebugCommands == true`** のときのみ有効。そのうえで **コンソール source=0** または **`command.tcg_debug` ACE**。`Config.DebugCommands ~= true` のときは **コンソールも含め一切実行不可**。

| コマンド | 説明（要約） |
|----------|----------------|
| `tcg_give` | card_id を1枚付与 |
| `tcg_giveall` | マスタ全種1枚ずつ |
| `tcg_givepack` | InitialCardRanks 由来のフリーをランダム付与（試行上限・同名上限考慮） |
| `tcg_reset` | プレイヤーデータ全削除＋対象クライアント強制クローズ |
| `tcg_clearcards` | 所持 + 全デッキの slot 削除（デッキ行は残る） |
| `tcg_dumpdeck` | アクティブデッキを JSON でコンソール出力 |
| `tcg_setrating` | rating 0〜9999 |
| `tcg_listplayers` | tcg_players 列挙 |
| `tcg_battleid` | 実行者の **サーバーID** を表示（仮想対戦の番号確認用） |

対象プレイヤーは **`target_server_id`（省略時は実行者）**。コンソールは対象 ID 必須。

### 12. ブラウザ単体テスト

- `IS_FIVEM` でなく、`mockDispatchServerEvent` で `bookData` 等を擬似送信
- **マスタ件数・所持件数は本番 `cards.lua` と一致しない**（UI 検証用に多め）— ファイル先頭コメントで明示済み

### 13. 実装フェーズ対応表（メモ）

| 区分 | 状態 |
|------|------|
| 1-1 初期化 | 済 |
| 1-2 DB | 済（＋拡張関数） |
| 1-3 collection | 済 |
| 1-4 deck | 済 |
| 1-5 クライアント・NUI 連携 | 済 |
| 1-6 NUI コレクション／デッキ | 済 |
| 1-7 デバッグ | 済（コマンドセットは改訂版テキストと完全一致ではない） |
| 1-8 mock | 済 |

### 14. 改訂版指示書との差分・未完了メモ（把握用）

- **自動保存**: **済** — `openBook` の `ui.autoSaveDebounceMs` で NUI に伝達、デッキの **＋／×** はデバウンス後に **キュー順次送信**。失敗時は **一覧同期（selectDeck）でロールバック**、ヘッダー **再同期** ボタンあり
- **デバッグ**: **済** — `Config.DebugCommands ~= true` のとき **`isDebugAllowed` が常に false**（コンソール含む）。ACE はその上位ゲートとして維持
- **シャッフルテスト**: **済（ローカル試行）** — モーダルに **「もう一度シャッフル」**、説明文でサーバー本番シャッフルと区別
- **ドキュメント**: ユーザー提示の **長文改訂版 `IMPLEMENTATION_PHASE1.md` 全文**は、リポ内の短い `docs/IMPLEMENTATION_PHASE1.md` とは **別物**—必要なら統合タスクとする

### 15. フェーズ2以降（予定の置き場）

**大項目の一覧・優先順位の議論の土台**は、冒頭 **「今後の PHASE ロードマップ（大項目）」** を正とする。ここでは対戦周りの **技術メモ** を残す。

- 対戦タブ・**3×3 盤**・**手札5**・ドロー・**サーバー側シャッフル**（ルールの文面は **「PHASE A 草案」**節を参照）
- **敗北時のコピー1枚付与**の本番実装・DC 時の裁定・パック排出設定 UI（いずれも別 PHASE）
- マッチング／ルーム（ロードマップ B）。当面は **サーバーID ロビー**で代替検証
- **トレードは廃止**。ランキング（D）は **仕様検討中**
- **レーティング更新**（Elo 等）の本実装（ロードマップ F）
- **マルチクライアント検証**（1PC 制約 → 別PC／将来ボット）
- **管理者マスタ CRUD と BOOK 整合**（ロードマップ G・必須条件参照）

### 16. 他リソースの開発ログとの関係

- **親リポジトリルートの `2026-04-30 開発日記.md`**（存在する場合）: **`jp-slot`** 向けの作業ログ。**jp-tcgbook とは無関係**。
- **本ファイル**: **`jp-tcgbook/` 専用**。tcgbook の設計累積と **2026-05-01 の画像ディレクトリ方針**をここにのみ記録する。

---

## 文字コード

本ファイルは **UTF-8（BOM なし）** で保存する。

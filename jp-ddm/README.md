# jp-DDM

パフォーマンス向け **モーション連続再生** と **YouTube 音楽** を同期させるクライアント専用ツールです。スクリプト名・ディレクトリ名は **`jp-ddm`**、表示名は **jp-DDM**（Dance / Direction / Music などの略称想定で利用可）。

- **依存なし**（ox_lib / QBCore 等不要）
- **サーバー保存なし** — プリセットは **各PCの FiveM クライアント KVS**（`SetResourceKvp`）のみ
- **NUI** 配分 **20% / 80%**（左: **カタログ＋カスタム**［▼折りたたみ可］ / 右: **2列**［左=セットリスト、右=スタート・YouTube・プリセット］）+ 再生中は左上ミニHUD
- **カタログ**の **「過去に入力したモーション」** — 下の **カスタム**から **＋リストに追加** した dict/clip を **KVS** に残し、カテゴリ **その他**（または **すべて**）の**末尾**に見出し付きで出す（`config` 固定カタログと同じは重複登録しない）
- **マークダウン** でプリセットを **エクスポート（コピー）/ インポート（ペースト）** — Discord 等で共有しやすい

---

## 保存について

データは **あなたのローカルPC** のリソース KVS にのみ保存されます。

- **PCを替えた・FiveMを再インストールした** 場合、他者のPC上のデータは **引き継がれません**。
- 共有したい場合は **📋 エクスポート** のテキストを相手に渡し、相手は **📥 インポート** で読み込みます。

---

## 導入

1. `resources` 配下に配置（例: `resources/[jp-mods]/jp-ddm/`）。
2. `server.cfg` に次を追加:

   ```cfg
   ensure jp-ddm
   ```

3. `refresh` 後 `ensure jp-ddm` またはサーバー再起動。

---

## コマンド

| コマンド | 説明 |
| -------- | ---- |
| `/ddm` | 管理画面（NUI）を開く。**スクリプトカメラは使わない**（ツール枠外の余白にゲーム映像。プレビュー/本番のモーションはワールドのまま再生） |
| `F12`（**デフォルト**、`config` の `ReopenManagerKey`・ゲーム内キー設定で変更可） | 本番（**最小化中**）のあと、**NUI フォーカス外しのためマウスでミニを操作できない**→ **F12 または `/ddm` で**管理画面に戻す |
| `/ddmstop` | モーション再生・音楽（YouTube）・ミニHUDを **即停止** |

チャットで `chat:addSuggestion` があると候補が出ます（未導入でもコマンドは有効）。

---

## 使い方（ざっくり）

1. `/ddm` → **左列**: **カタログ** の行を**クリックで試聴**、**＋追加**でセットに。同列の **カスタム** は **▼** で畳めます。  
2. **右側（広い枠）**は 2 列: **左サブ列にセットリスト**（**行クリック**で試聴、**🗑・秒**は従来どおり）、**右サブ列に** **YouTube**・**プリセット** など。  
3. **🎭 プレビュー** → 管理画面のまま**自キャラだけ**連続モーション（YouTube は鳴らさない）。右列に進行状況。  
4. **📌 最小化してスタート** → 従来の本番（NUI 非表示・**左上ミニHUD**、YouTube は非表示プレイヤーで再生）。  
5. **最小化中はマウス不可**（NUI フォーカスを外しゲーム操作に戻すため）→ ミニ枠内の案内のとおり **F12**（`config` で変更可）または **`/ddm`** で管理に戻すと再びマウスで操作可。  
6. **⏸ / ⏭ / 停止** は**プレビュー中は右サブ列**、**本番中は**まず 5. でフォーカスを戻すか、**左上ミニHUD**（戻ったあと）から。止めるだけなら **`/ddmstop`** も可。チーム共有は **エクスポート/インポート**（音声称もJSONに含まれる）。

### 画面・カメラ

スクリプトカメラは**使いません**。**ツールのパネル外**や窓枠外の部分でゲーム映像を見られ、モーションは従来どおり**ワールド上**で再生されます（解像度・向きはゲームカメラに依存）。

### エクスポート形式

人間可読の **表付きマークダウン** 末尾に、

```text
<!-- jp-ddm-data:（Base64 JSON） -->
```

を付与。インポートは **主にこの行** から正確に復元します。表部分だけ手編集しても、コメント行が生きていれば上書き読み込み可。

---

## トラブル

| 事象 | 想定 |
| ---- | ---- |
| YouTube が鳴らない | CEF の自動再生制限。ゲーム内では仕様。別ブラウザで同URLを一度開く、音量等。 |
| 管理画面は出るがアニメしない | `dict/clip` の誤り・サーバー未ストリーミング。F8 ログを確認。 |
| 他プレイヤーに見えない？ | クライアント完結のため、**他PCには同プリセット＋同タイミングで自前スタート**が必要（同期サーバーは無し）。 |
| エクスポートでコピー失敗？ | NUI では `navigator.clipboard` が使えないことがある。**execCommand 経由**で再試行し、だめなら**手動コピー用の窓**が開きます。 |

---

## ファイル構成

```text
jp-ddm/
├── config.lua
├── client/main.lua
├── html/index.html, style.css, app.js
├── fxmanifest.lua
└── README.md
```

- `config.lua` … コマンド名・カタログ `Config.Catalog`（運営向けに日本語コメントあり）
- サーバースクリプト **なし**

---

## 作者・バージョン

- `fxmanifest.lua` の `version` 参照
- 作者: **JP-Mods**

本リポジトリ方針の **日本向け FiveM 無料 MOD** として再配布・改変可。

# jp-DDM 用スクリーンショット

`../README.md` から **相対パス** `docs/ファイル名` で参照します。

次の 4 ファイルを **ここ（`jp-ddm/docs/`）** に置いてください。拡張子は **`.png` 推奨**（`.jpg` 等にする場合は `../README.md` 内のパスも合わせて書き換えてください）。

| ファイル | 掲載したい内容（目安） |
| -------- | -------------------- |
| `01-launch-command.png` | **起動コマンド**（例: チャットで `/ddm` 入力・補完・実行の様子、または F8） |
| `02-launch.png` | **起動**直後（管理画面 NUI が開いた全画面） |
| `03-import.png` | **エクスポートしたものをインポート**（📥 モーダルに貼り付け中・読み込み前後など） |
| `04-minimized.png` | **最小化して本番中**（左上ミニHUD・必要なら F12 案内が見える状態） |

未配置のとき、GitHub 上の README では画像枠が壊れ表示になることがあります。画像をコミットすると表示されます。

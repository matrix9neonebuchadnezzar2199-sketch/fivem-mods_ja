# 2026-05-04 開発計画（jp-mch）

本ファイルは UTF-8（BOM なし）で保存する。

---

## 運用ルール（本モッド共通）

- **作業開始前**: 当日（または作業単位）の「開発計画」ファイルを先に作成し、目的・スコープ・完了条件・触るファイルを書く。
- **作業終了後**: 同じ日付の「開発日記」に、実施内容・差分の要点・確認結果・Git 情報を追記する。
- **ファイル名の例**: `YYYY-MM-DD 開発計画.md` / `YYYY-MM-DD 開発日記.md`（モッド直下に置く）

---

## 本日の計画（jp-mch 初回実装）

**目的**: Munlay Clean HUD 系をベースに、リポジトリ規約に合う **jp-mch**（standalone・FW 非依存の読み取り層・日本語デフォルト）として新規リソースを追加する。

**スコープ**

- `fxmanifest.lua`（`lua54 'yes'`、`shared_scripts`、`bridge` + `client/main.lua`、`files` は `html/index.html` のみ）
- `config.lua`（全項目に日本語コメント、`Config.Framework` 等）
- `locales/ja.lua`（`Locale.ui` / `Locale.log`）
- `bridge/framework.lua`（qbox / qb / esx / standalone、旧 `hud:*` 互換）
- `client/main.lua`（KVP `jp_mch_*`、旧 `munlay_hud_*` マイグレーション、NUI `setI18n`）
- `html/index.html`（日本語 UI・既存 NUI ロジックの維持）
- `README.md`（日本語）
- 旧ルートの `client.lua` は廃止（`client/main.lua` に統合）

**完了条件**

- 上記ファイルがモッド直下構成で揃い、UTF-8（BOM なし）で保存されている
- 親リポジトリで `jp-mch/` がコミット・push 可能な状態

**完了条件（実機・任意）**

- FiveM で `ensure jp-mch` し、F8 にフレームワーク検出ログが出ること

**触るファイル / 新規**

- 新規: `config.lua`, `locales/ja.lua`, `bridge/framework.lua`, `client/main.lua`, 上記 README・日記・本計画
- 更新: `fxmanifest.lua`, `html/index.html`（必要箇所のみ）
- 削除: ルート `client.lua`（存在する場合）

---

## メモ（初回分）

本計画ファイルは、運用ルール提示に合わせて **初回実装作業後** に内容を整理して作成した（以降の作業では「計画 → 実装 → 日記」の順を守る）。
